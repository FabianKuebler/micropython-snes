diff --git a/extmod/modframebuf.c b/extmod/modframebuf.c
index 593125aa1..82cdfb280 100644
--- a/extmod/modframebuf.c
+++ b/extmod/modframebuf.c
@@ -68,13 +68,13 @@ typedef struct _mp_framebuf_p_t {
 // Functions for MHLSB and MHMSB
 
 static void mono_horiz_setpixel(const mp_obj_framebuf_t *fb, unsigned int x, unsigned int y, uint32_t col) {
-    size_t index = (x + y * fb->stride) >> 3;
+    size_t index = ((size_t)x + (size_t)y * (size_t)fb->stride) >> 3;
     unsigned int offset = fb->format == FRAMEBUF_MHMSB ? x & 0x07 : 7 - (x & 0x07);
     ((uint8_t *)fb->buf)[index] = (((uint8_t *)fb->buf)[index] & ~(0x01 << offset)) | ((col != 0) << offset);
 }
 
 static uint32_t mono_horiz_getpixel(const mp_obj_framebuf_t *fb, unsigned int x, unsigned int y) {
-    size_t index = (x + y * fb->stride) >> 3;
+    size_t index = ((size_t)x + (size_t)y * (size_t)fb->stride) >> 3;
     unsigned int offset = fb->format == FRAMEBUF_MHMSB ? x & 0x07 : 7 - (x & 0x07);
     return (((uint8_t *)fb->buf)[index] >> (offset)) & 0x01;
 }
@@ -120,15 +120,15 @@ static void mvlsb_fill_rect(const mp_obj_framebuf_t *fb, unsigned int x, unsigne
 // Functions for RGB565 format
 
 static void rgb565_setpixel(const mp_obj_framebuf_t *fb, unsigned int x, unsigned int y, uint32_t col) {
-    ((uint16_t *)fb->buf)[x + y * fb->stride] = col;
+    ((uint16_t *)fb->buf)[(size_t)x + (size_t)y * (size_t)fb->stride] = col;
 }
 
 static uint32_t rgb565_getpixel(const mp_obj_framebuf_t *fb, unsigned int x, unsigned int y) {
-    return ((uint16_t *)fb->buf)[x + y * fb->stride];
+    return ((uint16_t *)fb->buf)[(size_t)x + (size_t)y * (size_t)fb->stride];
 }
 
 static void rgb565_fill_rect(const mp_obj_framebuf_t *fb, unsigned int x, unsigned int y, unsigned int w, unsigned int h, uint32_t col) {
-    uint16_t *b = &((uint16_t *)fb->buf)[x + y * fb->stride];
+    uint16_t *b = &((uint16_t *)fb->buf)[(size_t)x + (size_t)y * (size_t)fb->stride];
     while (h--) {
         for (unsigned int ww = w; ww; --ww) {
             *b++ = col;
@@ -140,7 +140,7 @@ static void rgb565_fill_rect(const mp_obj_framebuf_t *fb, unsigned int x, unsign
 // Functions for GS2_HMSB format
 
 static void gs2_hmsb_setpixel(const mp_obj_framebuf_t *fb, unsigned int x, unsigned int y, uint32_t col) {
-    uint8_t *pixel = &((uint8_t *)fb->buf)[(x + y * fb->stride) >> 2];
+    uint8_t *pixel = &((uint8_t *)fb->buf)[((size_t)x + (size_t)y * (size_t)fb->stride) >> 2];
     uint8_t shift = (x & 0x3) << 1;
     uint8_t mask = 0x3 << shift;
     uint8_t color = (col & 0x3) << shift;
@@ -148,7 +148,7 @@ static void gs2_hmsb_setpixel(const mp_obj_framebuf_t *fb, unsigned int x, unsig
 }
 
 static uint32_t gs2_hmsb_getpixel(const mp_obj_framebuf_t *fb, unsigned int x, unsigned int y) {
-    uint8_t pixel = ((uint8_t *)fb->buf)[(x + y * fb->stride) >> 2];
+    uint8_t pixel = ((uint8_t *)fb->buf)[((size_t)x + (size_t)y * (size_t)fb->stride) >> 2];
     uint8_t shift = (x & 0x3) << 1;
     return (pixel >> shift) & 0x3;
 }
@@ -164,7 +164,7 @@ static void gs2_hmsb_fill_rect(const mp_obj_framebuf_t *fb, unsigned int x, unsi
 // Functions for GS4_HMSB format
 
 static void gs4_hmsb_setpixel(const mp_obj_framebuf_t *fb, unsigned int x, unsigned int y, uint32_t col) {
-    uint8_t *pixel = &((uint8_t *)fb->buf)[(x + y * fb->stride) >> 1];
+    uint8_t *pixel = &((uint8_t *)fb->buf)[((size_t)x + (size_t)y * (size_t)fb->stride) >> 1];
 
     if (x % 2) {
         *pixel = ((uint8_t)col & 0x0f) | (*pixel & 0xf0);
@@ -175,15 +175,15 @@ static void gs4_hmsb_setpixel(const mp_obj_framebuf_t *fb, unsigned int x, unsig
 
 static uint32_t gs4_hmsb_getpixel(const mp_obj_framebuf_t *fb, unsigned int x, unsigned int y) {
     if (x % 2) {
-        return ((uint8_t *)fb->buf)[(x + y * fb->stride) >> 1] & 0x0f;
+        return ((uint8_t *)fb->buf)[((size_t)x + (size_t)y * (size_t)fb->stride) >> 1] & 0x0f;
     }
 
-    return ((uint8_t *)fb->buf)[(x + y * fb->stride) >> 1] >> 4;
+    return ((uint8_t *)fb->buf)[((size_t)x + (size_t)y * (size_t)fb->stride) >> 1] >> 4;
 }
 
 static void gs4_hmsb_fill_rect(const mp_obj_framebuf_t *fb, unsigned int x, unsigned int y, unsigned int w, unsigned int h, uint32_t col) {
     col &= 0x0f;
-    uint8_t *pixel_pair = &((uint8_t *)fb->buf)[(x + y * fb->stride) >> 1];
+    uint8_t *pixel_pair = &((uint8_t *)fb->buf)[((size_t)x + (size_t)y * (size_t)fb->stride) >> 1];
     uint8_t col_shifted_left = col << 4;
     uint8_t col_pixel_pair = col_shifted_left | col;
     unsigned int pixel_count_till_next_line = (fb->stride - w) >> 1;
@@ -215,16 +215,16 @@ static void gs4_hmsb_fill_rect(const mp_obj_framebuf_t *fb, unsigned int x, unsi
 // Functions for GS8 format
 
 static void gs8_setpixel(const mp_obj_framebuf_t *fb, unsigned int x, unsigned int y, uint32_t col) {
-    uint8_t *pixel = &((uint8_t *)fb->buf)[(x + y * fb->stride)];
+    uint8_t *pixel = &((uint8_t *)fb->buf)[((size_t)x + (size_t)y * (size_t)fb->stride)];
     *pixel = col & 0xff;
 }
 
 static uint32_t gs8_getpixel(const mp_obj_framebuf_t *fb, unsigned int x, unsigned int y) {
-    return ((uint8_t *)fb->buf)[(x + y * fb->stride)];
+    return ((uint8_t *)fb->buf)[((size_t)x + (size_t)y * (size_t)fb->stride)];
 }
 
 static void gs8_fill_rect(const mp_obj_framebuf_t *fb, unsigned int x, unsigned int y, unsigned int w, unsigned int h, uint32_t col) {
-    uint8_t *pixel = &((uint8_t *)fb->buf)[(x + y * fb->stride)];
+    uint8_t *pixel = &((uint8_t *)fb->buf)[((size_t)x + (size_t)y * (size_t)fb->stride)];
     while (h--) {
         memset(pixel, col, w);
         pixel += fb->stride;
@@ -918,7 +918,7 @@ static const mp_rom_map_elem_t framebuf_module_globals_table[] = {
 
 static MP_DEFINE_CONST_DICT(framebuf_module_globals, framebuf_module_globals_table);
 
-const mp_obj_module_t mp_module_framebuf = {
+const mp_obj_module_t MICROPY_OBJ_BASE_ALIGNMENT mp_module_framebuf = {
     .base = { &mp_type_module },
     .globals = (mp_obj_dict_t *)&framebuf_module_globals,
 };
diff --git a/py/bc.c b/py/bc.c
index 7669365ce..64e7373ca 100644
--- a/py/bc.c
+++ b/py/bc.c
@@ -301,6 +301,9 @@ static void mp_setup_code_state_helper(mp_code_state_t *code_state, size_t n_arg
     const uint8_t *ip = code_state->ip + n_info;
 
     // bytecode prelude: initialise closed over variables
+    #ifdef BC_DEBUG
+    size_t dbg_n_cell = n_cell;
+    #endif
     for (; n_cell; --n_cell) {
         size_t local_num = *ip++;
         code_state_state[n_state - 1 - local_num] =
@@ -310,6 +313,33 @@ static void mp_setup_code_state_helper(mp_code_state_t *code_state, size_t n_arg
     // now that we skipped over the prelude, set the ip for the VM
     code_state->ip = ip;
 
+    #ifdef BC_DEBUG
+    if (n_args > 0 && code_state_state[n_state - 1] != args[0]) {
+        extern void mb_puts(const char *s);
+        extern void mb_puthex32(unsigned long v);
+        union { mp_obj_t o; unsigned char b[4]; } u;
+        mb_puts("[bc! nst=");
+        mb_puthex32(n_state);
+        mb_puts(" na=");
+        mb_puthex32(n_args);
+        mb_puts(" npos=");
+        mb_puthex32(n_pos_args);
+        mb_puts(" nkw=");
+        mb_puthex32(n_kwonly_args);
+        mb_puts(" ncell=");
+        mb_puthex32(dbg_n_cell);
+        mb_puts(" ninfo=");
+        mb_puthex32(n_info);
+        mb_puts(" s0=");
+        u.o = code_state_state[n_state - 1];
+        mb_puthex32(u.b[2]); mb_puthex32(u.b[1]); mb_puthex32(u.b[0]);
+        mb_puts(" a0=");
+        u.o = args[0];
+        mb_puthex32(u.b[2]); mb_puthex32(u.b[1]); mb_puthex32(u.b[0]);
+        mb_puts("]\n");
+    }
+    #endif
+
     DEBUG_printf("Calling: n_pos_args=%d, n_kwonly_args=%d\n", n_pos_args, n_kwonly_args);
     dump_args(code_state_state + n_state - n_pos_args - n_kwonly_args, n_pos_args + n_kwonly_args);
     dump_args(code_state_state, n_state);
diff --git a/py/binary.c b/py/binary.c
index ef2857b43..3b9fcd87d 100644
--- a/py/binary.c
+++ b/py/binary.c
@@ -299,7 +299,7 @@ mp_obj_t mp_binary_get_val_array(char typecode, void *p, size_t index) {
 // The long long type is guaranteed to hold at least 64 bits, and size is at
 // most 8 (for q and Q), so we will always be able to parse the given data
 // and fit it into a long long.
-long long mp_binary_get_int(size_t size, bool is_signed, bool big_endian, const byte *src) {
+mp_binary_int_t mp_binary_get_int(size_t size, bool is_signed, bool big_endian, const byte *src) {
     int delta;
     if (!big_endian) {
         delta = -1;
@@ -308,7 +308,15 @@ long long mp_binary_get_int(size_t size, bool is_signed, bool big_endian, const
         delta = 1;
     }
 
+    #if defined(__VBCC__)
+    // vbcc 65816 has no 64-bit arithmetic runtime. mp_int_t is 32-bit and
+    // small ints are <=31 bits, so accumulate in 32 bits; >4-byte fields
+    // ('q'/'Q', already excluded with LONGINT_IMPL NONE) would truncate. The
+    // 32->long long widening on return needs no helper.
+    mp_uint_t val = 0;
+    #else
     unsigned long long val = 0;
+    #endif
     if (is_signed && *src & 0x80) {
         val = -1;
     }
@@ -322,6 +330,11 @@ long long mp_binary_get_int(size_t size, bool is_signed, bool big_endian, const
 }
 
 #define is_signed(typecode) (typecode > 'Z')
+// On vbcc mp_binary_int_t is mp_int_t (32-bit), so these are plain casts. On
+// other compilers the operand is a long long and these narrow it.
+#define MP_LL_TO_U32(v) ((mp_uint_t)(v))
+#define MP_LL_TO_I32(v) ((mp_int_t)(v))
+
 mp_obj_t mp_binary_get_val(char struct_type, char val_type, byte *p_base, byte **ptr) {
     byte *p = *ptr;
     size_t align;
@@ -338,12 +351,12 @@ mp_obj_t mp_binary_get_val(char struct_type, char val_type, byte *p_base, byte *
     }
     *ptr = p + size;
 
-    long long val = mp_binary_get_int(size, is_signed(val_type), (struct_type == '>'), p);
+    mp_binary_int_t val = mp_binary_get_int(size, is_signed(val_type), (struct_type == '>'), p);
 
     if (MICROPY_PY_STRUCT_UNSAFE_TYPECODES && val_type == 'O') {
-        return (mp_obj_t)(mp_uint_t)val;
+        return (mp_obj_t)MP_LL_TO_U32(val);
     } else if (MICROPY_PY_STRUCT_UNSAFE_TYPECODES && val_type == 'S') {
-        const char *s_val = (const char *)(uintptr_t)(mp_uint_t)val;
+        const char *s_val = (const char *)(uintptr_t)MP_LL_TO_U32(val);
         return mp_obj_new_str_from_cstr(s_val);
     #if MICROPY_PY_BUILTINS_FLOAT
     } else if (val_type == 'e') {
@@ -362,17 +375,28 @@ mp_obj_t mp_binary_get_val(char struct_type, char val_type, byte *p_base, byte *
         return mp_obj_new_float_from_d(fpu.f);
     #endif
     } else if (is_signed(val_type)) {
+        #if defined(__VBCC__)
+        // No 64-bit compares on vbcc; the value already fits 32 bits (>4-byte
+        // typecodes are excluded with LONGINT_IMPL NONE), so take the small-int
+        // path directly.
+        return mp_obj_new_int(MP_LL_TO_I32(val));
+        #else
         if ((long long)MP_SMALL_INT_MIN <= val && val <= (long long)MP_SMALL_INT_MAX) {
-            return mp_obj_new_int((mp_int_t)val);
+            return mp_obj_new_int(MP_LL_TO_I32(val));
         } else {
             return mp_obj_new_int_from_ll(val);
         }
+        #endif
     } else {
+        #if defined(__VBCC__)
+        return mp_obj_new_int_from_uint(MP_LL_TO_U32(val));
+        #else
         if ((unsigned long long)val <= (unsigned long long)MP_SMALL_INT_MAX) {
-            return mp_obj_new_int_from_uint((mp_uint_t)val);
+            return mp_obj_new_int_from_uint(MP_LL_TO_U32(val));
         } else {
             return mp_obj_new_int_from_ull(val);
         }
+        #endif
     }
 }
 
diff --git a/py/binary.h b/py/binary.h
index 5c645bcaa..3f51647ff 100644
--- a/py/binary.h
+++ b/py/binary.h
@@ -40,7 +40,15 @@ void mp_binary_set_val_array(char typecode, void *p, size_t index, mp_obj_t val_
 void mp_binary_set_val_array_from_int(char typecode, void *p, size_t index, mp_int_t val);
 mp_obj_t mp_binary_get_val(char struct_type, char val_type, byte *p_base, byte **ptr);
 void mp_binary_set_val(char struct_type, char val_type, mp_obj_t val_in, byte *p_base, byte **ptr);
-long long mp_binary_get_int(size_t size, bool is_signed, bool big_endian, const byte *src);
+// Returns long long (up to 8-byte 'q'/'Q' fields). vbcc 65816 has no 64-bit
+// runtime, so there it returns mp_int_t (32-bit) — >4-byte typecodes are
+// excluded anyway with LONGINT_IMPL NONE.
+#if defined(__VBCC__)
+typedef mp_int_t mp_binary_int_t;
+#else
+typedef long long mp_binary_int_t;
+#endif
+mp_binary_int_t mp_binary_get_int(size_t size, bool is_signed, bool big_endian, const byte *src);
 void mp_binary_set_int(size_t val_sz, bool big_endian, byte *dest, mp_uint_t val);
 
 #endif // MICROPY_INCLUDED_PY_BINARY_H
diff --git a/py/emit.h b/py/emit.h
index 033ac9c76..e532cd80b 100644
--- a/py/emit.h
+++ b/py/emit.h
@@ -191,7 +191,15 @@ static inline size_t mp_emit_common_alloc_const_child(mp_emit_common_t *emit, mp
 }
 
 static inline void mp_emit_common_get_id_for_load(scope_t *scope, qstr qst) {
+    #if !defined(__VBCC__)
+    // vbcc emits unused static inlines (clang discards them); this compiler-only
+    // helper would then pull in scope_find_or_add_id, which the bytecode-VM-only
+    // build does not link (MICROPY_ENABLE_COMPILER is 0).
     scope_find_or_add_id(scope, qst, ID_INFO_KIND_GLOBAL_IMPLICIT);
+    #else
+    (void)scope;
+    (void)qst;
+    #endif
 }
 
 id_info_t *mp_emit_common_get_id_for_modification(scope_t *scope, qstr qst);
diff --git a/py/emitbc.c b/py/emitbc.c
index 0fbda56fd..aa796e8c0 100644
--- a/py/emitbc.c
+++ b/py/emitbc.c
@@ -244,7 +244,12 @@ static void emit_write_bytecode_byte_label(emit_t *emit, int stack_adj, byte b1,
     // Compute the jump size and offset only when code size is known.
     if (emit->pass >= MP_PASS_CODE_SIZE) {
         // The -2 accounts for this jump opcode taking 2 bytes (at least).
-        bytecode_offset = emit->label_offsets[label] - emit->bytecode_offset - 2;
+        // Widen BEFORE subtracting: with size_t narrower than ssize_t
+        // (16-bit size_t here) the unsigned difference has no sign bit to
+        // wrap into, and a backward jump of -20 arrives as +65516 -> every
+        // loop raised "bytecode overflow".
+        bytecode_offset = (ssize_t)emit->label_offsets[label]
+            - (ssize_t)emit->bytecode_offset - 2;
 
         // Check if the bytecode_offset is small enough to use a 1-byte encoding.
         if ((is_signed && -64 <= bytecode_offset && bytecode_offset <= 63)
@@ -273,6 +278,23 @@ static void emit_write_bytecode_byte_label(emit_t *emit, int stack_adj, byte b1,
             bytecode_offset += 0x4000;
         }
         if (emit->pass == MP_PASS_EMIT && !(0 <= bytecode_offset && bytecode_offset <= 0x7fff)) {
+            #ifdef EMIT_DEBUG
+            {
+                extern void mb_puts(const char *s);
+                extern void mb_puthex32(unsigned long v);
+                mb_puts("[ovf lbl=");
+                mb_puthex32(label);
+                mb_puts(" lofs=");
+                mb_puthex32(emit->label_offsets[label]);
+                mb_puts(" bofs=");
+                mb_puthex32(emit->bytecode_offset);
+                mb_puts(" delta=");
+                mb_puthex32((unsigned long)(long)bytecode_offset);
+                mb_puts(" op=");
+                mb_puthex32(b1);
+                mb_puts("]\n");
+            }
+            #endif
             emit->overflow = true;
         }
         c[1] = 0x80 | (bytecode_offset & 0x7f);
diff --git a/py/formatfloat.c b/py/formatfloat.c
index 1ea34f84b..2bb7785f8 100644
--- a/py/formatfloat.c
+++ b/py/formatfloat.c
@@ -65,7 +65,7 @@
 
 static inline int fp_expval(mp_float_t x) {
     mp_float_union_t fb = { x };
-    return (int)fb.p.exp - MP_FLOAT_EXP_OFFSET;
+    return MP_FLOAT_UNION_EXP(fb) - MP_FLOAT_EXP_OFFSET;
 }
 
 #if MICROPY_FLOAT_IMPL == MICROPY_FLOAT_IMPL_DOUBLE
diff --git a/py/gc.c b/py/gc.c
index 5fe26ef89..da6aa19ae 100644
--- a/py/gc.c
+++ b/py/gc.c
@@ -186,8 +186,19 @@ static void gc_setup_area(mp_state_mem_area_t *area, void *start, void *end) {
 
     // Allocate the GC pool of heap blocks.
     size_t gc_pool_block_len = area->gc_alloc_table_byte_len * BLOCKS_PER_ATB;
-    area->gc_pool_start = (byte *)end - gc_pool_block_len * BYTES_PER_BLOCK;
-    area->gc_pool_end = end;
+    // Derive the pool pointers from `start` plus scalar offsets rather than
+    // from `end`: Calypsi 65816 has been observed to truncate the bank byte
+    // of the third pointer parameter when marshalling this call, so `end`
+    // cannot be trusted as a pointer here (its low 16 bits -- and therefore
+    // total_byte_len above -- are correct). Equivalent arithmetic on all
+    // platforms.
+    {
+        size_t pool_bytes = gc_pool_block_len * BYTES_PER_BLOCK;
+        byte *pool_start = area->gc_alloc_table_start + (total_byte_len - pool_bytes);
+        area->gc_pool_start = pool_start;
+        area->gc_pool_end = pool_start + pool_bytes;
+    }
+    (void)end;
     assert(area->gc_pool_start >= next_table);
 
     // Clear ATB's, and FTB's and WTB's if they are enabled.
@@ -443,10 +454,23 @@ void gc_collect_start(void) {
     // Trace root pointers.  This relies on the root pointers being organised
     // correctly in the mp_state_ctx structure.  We scan nlr_top, dict_locals,
     // dict_globals, then the root pointer section of mp_state_vm.
-    void **ptrs = (void **)(void *)&mp_state_ctx;
+    // NB: byte arithmetic for the section base: on compilers that pack
+    // structs without padding (Calypsi 65816: 16-bit size_t, no alignment)
+    // root_start need not be a multiple of sizeof(void *), and pointer
+    // fields WITHIN the section can sit at any 2-byte offset (embedded
+    // size_t/uint16_t fields shift everything after them). Scan the section
+    // twice, phase-shifted by 2 bytes, so every 2-aligned pointer-sized
+    // field is visited; the extra scan only over-marks (conservative GC).
     size_t root_start = offsetof(mp_state_ctx_t, thread.dict_locals);
-    size_t root_end = offsetof(mp_state_ctx_t, vm.qstr_last_chunk);
-    gc_collect_root(ptrs + root_start / sizeof(void *), (root_end - root_start) / sizeof(void *));
+    // include qstr_last_chunk itself: a fresh chunk with no interned qstr
+    // yet has no pool entry pointing into it, so this root is its only
+    // reference (upstream excludes it and relies on pool reachability)
+    size_t root_end = offsetof(mp_state_ctx_t, vm.qstr_last_chunk) + sizeof(char *);
+    void **ptrs = (void **)(void *)((char *)&mp_state_ctx + root_start);
+    gc_collect_root(ptrs, (root_end - root_start) / sizeof(void *));
+    #if MICROPY_GC_UNALIGNED_ROOT_SECTION
+    gc_collect_root((void **)((char *)ptrs + 2), (root_end - root_start - 2) / sizeof(void *));
+    #endif
 
     #if MICROPY_ENABLE_PYSTACK
     // Trace root pointers from the Python stack.
@@ -520,8 +544,22 @@ static void gc_mark_subtree(size_t block)
         assert(area->gc_pool_start + (block + n_blocks) * BYTES_PER_BLOCK <= area->gc_pool_end);
 
         // check this block's children
+        // On packed 16-bit-size_t builds (Calypsi 65816) heap objects hold
+        // pointers at 2-mod-4 offsets too (inline tuple items after a
+        // 2-byte len, qstr pool entry arrays, ...). Scan each block twice,
+        // phase-shifted by 2 bytes, like the root-section scan: a missed
+        // child pointer means the collector frees live objects (observed:
+        // the current qstr chunk, reached only via pool entries at odd
+        // phase, was reused as a code_state and interning corrupted it).
+        // The extra pass only over-marks (conservative GC).
+        #if MICROPY_GC_UNALIGNED_ROOT_SECTION
+        for (size_t phase = 0; phase <= 2; phase += 2) {
+        void **ptrs = (void **)(void *)((char *)PTR_FROM_BLOCK(area, block) + phase);
+        for (size_t i = (n_blocks * BYTES_PER_BLOCK - phase) / sizeof(void *); i > 0; i--, ptrs++) {
+        #else
         void **ptrs = (void **)PTR_FROM_BLOCK(area, block);
         for (size_t i = n_blocks * BYTES_PER_BLOCK / sizeof(void *); i > 0; i--, ptrs++) {
+        #endif
             MICROPY_GC_HOOK_LOOP(i);
             void *ptr = *ptrs;
             // If this is a heap pointer that hasn't been marked, mark it and push
@@ -556,6 +594,9 @@ static void gc_mark_subtree(size_t block)
                 MP_STATE_MEM(gc_stack_overflow) = 1;
             }
         }
+        #if MICROPY_GC_UNALIGNED_ROOT_SECTION
+        }
+        #endif
 
         // Are there any blocks on the stack?
         if (sp == 0) {
@@ -844,21 +885,13 @@ void gc_weakref_mark(void *ptr) {
 }
 #endif
 
-void *gc_alloc(size_t n_bytes, unsigned int alloc_flags) {
-    bool has_finaliser = alloc_flags & GC_ALLOC_FLAG_HAS_FINALISER;
-    size_t n_blocks = ((n_bytes + BYTES_PER_BLOCK - 1) & (~(BYTES_PER_BLOCK - 1))) / BYTES_PER_BLOCK;
-    DEBUG_printf("gc_alloc(" UINT_FMT " bytes -> " UINT_FMT " blocks)\n", n_bytes, n_blocks);
-
-    // check for 0 allocation
-    if (n_blocks == 0) {
-        return NULL;
-    }
-
-    // check if GC is locked
-    if (MP_STATE_THREAD(gc_lock_depth) > 0) {
-        return NULL;
-    }
-
+// One allocation attempt: search for a free run and commit it, or return
+// NULL. Split out of gc_alloc so that a retry after gc_collect() re-derives
+// ALL state in a fresh invocation — compilers with fragile caller-saved
+// register allocation (Calypsi 65816) lost cached pointers (e.g. the heap
+// area) across the internal gc_collect() call in the original single-body
+// version, making the post-collect retry compute wild addresses.
+static void *gc_alloc_attempt(size_t n_bytes, size_t n_blocks, bool has_finaliser) {
     GC_ENTER();
 
     mp_state_mem_area_t *area;
@@ -866,69 +899,41 @@ void *gc_alloc(size_t n_bytes, unsigned int alloc_flags) {
     size_t end_block;
     size_t start_block;
     size_t n_free;
-    int collected = !MP_STATE_MEM(gc_auto_collect_enabled);
-    #if MICROPY_GC_SPLIT_HEAP_AUTO
-    bool added = false;
-    #endif
 
-    #if MICROPY_GC_ALLOC_THRESHOLD
-    if (!collected && MP_STATE_MEM(gc_alloc_amount) >= MP_STATE_MEM(gc_alloc_threshold)) {
-        GC_EXIT();
-        gc_collect();
-        collected = 1;
-        GC_ENTER();
-    }
+    #if MICROPY_GC_SPLIT_HEAP
+    area = MP_STATE_MEM(gc_last_free_area);
+    #else
+    area = &MP_STATE_MEM(area);
     #endif
 
-    for (;;) {
-
-        #if MICROPY_GC_SPLIT_HEAP
-        area = MP_STATE_MEM(gc_last_free_area);
-        #else
-        area = &MP_STATE_MEM(area);
-        #endif
-
-        // look for a run of n_blocks available blocks
-        for (; area != NULL; area = NEXT_AREA(area), i = 0) {
-            n_free = 0;
-            for (i = area->gc_last_free_atb_index; i < area->gc_alloc_table_byte_len; i++) {
-                MICROPY_GC_HOOK_LOOP(i);
-                byte a = area->gc_alloc_table_start[i];
-                // *FORMAT-OFF*
-                if (ATB_0_IS_FREE(a)) { if (++n_free >= n_blocks) { i = i * BLOCKS_PER_ATB + 0; goto found; } } else { n_free = 0; }
-                if (ATB_1_IS_FREE(a)) { if (++n_free >= n_blocks) { i = i * BLOCKS_PER_ATB + 1; goto found; } } else { n_free = 0; }
-                if (ATB_2_IS_FREE(a)) { if (++n_free >= n_blocks) { i = i * BLOCKS_PER_ATB + 2; goto found; } } else { n_free = 0; }
-                if (ATB_3_IS_FREE(a)) { if (++n_free >= n_blocks) { i = i * BLOCKS_PER_ATB + 3; goto found; } } else { n_free = 0; }
-                // *FORMAT-ON*
-            }
-
-            // No free blocks found on this heap. Mark this heap as
-            // filled, so we won't try to find free space here again until
-            // space is freed.
-            #if MICROPY_GC_SPLIT_HEAP
-            if (n_blocks == 1) {
-                area->gc_last_free_atb_index = (i + 1) / BLOCKS_PER_ATB; // or (size_t)-1
-            }
-            #endif
+    // look for a run of n_blocks available blocks
+    for (; area != NULL; area = NEXT_AREA(area), i = 0) {
+        n_free = 0;
+        for (i = area->gc_last_free_atb_index; i < area->gc_alloc_table_byte_len; i++) {
+            MICROPY_GC_HOOK_LOOP(i);
+            byte a = area->gc_alloc_table_start[i];
+            // *FORMAT-OFF*
+            if (ATB_0_IS_FREE(a)) { if (++n_free >= n_blocks) { i = i * BLOCKS_PER_ATB + 0; goto found; } } else { n_free = 0; }
+            if (ATB_1_IS_FREE(a)) { if (++n_free >= n_blocks) { i = i * BLOCKS_PER_ATB + 1; goto found; } } else { n_free = 0; }
+            if (ATB_2_IS_FREE(a)) { if (++n_free >= n_blocks) { i = i * BLOCKS_PER_ATB + 2; goto found; } } else { n_free = 0; }
+            if (ATB_3_IS_FREE(a)) { if (++n_free >= n_blocks) { i = i * BLOCKS_PER_ATB + 3; goto found; } } else { n_free = 0; }
+            // *FORMAT-ON*
         }
 
-        GC_EXIT();
-        // nothing found!
-        if (collected) {
-            #if MICROPY_GC_SPLIT_HEAP_AUTO
-            if (!added && gc_try_add_heap(n_bytes)) {
-                added = true;
-                continue;
-            }
-            #endif
-            return NULL;
+        // No free blocks found on this heap. Mark this heap as
+        // filled, so we won't try to find free space here again until
+        // space is freed.
+        #if MICROPY_GC_SPLIT_HEAP
+        if (n_blocks == 1) {
+            area->gc_last_free_atb_index = (i + 1) / BLOCKS_PER_ATB; // or (size_t)-1
         }
-        DEBUG_printf("gc_alloc(" UINT_FMT "): no free mem, triggering GC\n", n_bytes);
-        gc_collect();
-        collected = 1;
-        GC_ENTER();
+        #endif
     }
 
+    GC_EXIT();
+    // nothing found!
+    return NULL;
+
     // found, ending at block i inclusive
 found:
     // get starting and end blocks, both inclusive
@@ -1001,6 +1006,58 @@ found:
     return ret_ptr;
 }
 
+// Called via a volatile function pointer so the attempt CANNOT be inlined
+// back into gc_alloc (Calypsi ignores noinline; inlining would reintroduce
+// the stale-cached-pointer-across-gc_collect bug this split exists to fix).
+static void *(*volatile gc_alloc_attempt_fn)(size_t, size_t, bool) = gc_alloc_attempt;
+
+void *gc_alloc(size_t n_bytes, unsigned int alloc_flags) {
+    bool has_finaliser = alloc_flags & GC_ALLOC_FLAG_HAS_FINALISER;
+    size_t n_blocks = ((n_bytes + BYTES_PER_BLOCK - 1) & (~(BYTES_PER_BLOCK - 1))) / BYTES_PER_BLOCK;
+    DEBUG_printf("gc_alloc(" UINT_FMT " bytes -> " UINT_FMT " blocks)\n", n_bytes, n_blocks);
+
+    // check for 0 allocation
+    if (n_blocks == 0) {
+        return NULL;
+    }
+
+    // check if GC is locked
+    if (MP_STATE_THREAD(gc_lock_depth) > 0) {
+        return NULL;
+    }
+
+    int collected = !MP_STATE_MEM(gc_auto_collect_enabled);
+    #if MICROPY_GC_SPLIT_HEAP_AUTO
+    bool added = false;
+    #endif
+
+    #if MICROPY_GC_ALLOC_THRESHOLD
+    if (!collected && MP_STATE_MEM(gc_alloc_amount) >= MP_STATE_MEM(gc_alloc_threshold)) {
+        gc_collect();
+        collected = 1;
+    }
+    #endif
+
+    for (;;) {
+        void *ret_ptr = gc_alloc_attempt_fn(n_bytes, n_blocks, has_finaliser);
+        if (ret_ptr != NULL) {
+            return ret_ptr;
+        }
+        if (collected) {
+            #if MICROPY_GC_SPLIT_HEAP_AUTO
+            if (!added && gc_try_add_heap(n_bytes)) {
+                added = true;
+                continue;
+            }
+            #endif
+            return NULL;
+        }
+        DEBUG_printf("gc_alloc(" UINT_FMT "): no free mem, triggering GC\n", n_bytes);
+        gc_collect();
+        collected = 1;
+    }
+}
+
 // force the freeing of a piece of memory
 // TODO: freeing here does not call finaliser
 void gc_free(void *ptr) {
diff --git a/py/misc.h b/py/misc.h
index a9c02a4dd..18d0240c9 100644
--- a/py/misc.h
+++ b/py/misc.h
@@ -81,7 +81,9 @@ typedef unsigned int uint;
 // do, others only with -Werror=vla, msvc always does.
 // The (void) is needed to avoid "left operand of comma operator has no effect [-Werror=unused-value]"
 // when using this macro on the left-hand side of a comma.
-#if defined(_MSC_VER) || defined(__cplusplus)
+#if defined(_MSC_VER) || defined(__cplusplus) || defined(__CALYPSI__) || defined(__VBCC__)
+// vbcc, like Calypsi, mishandles the non-constexpr VLA-sizeof trick used here
+// (it is a compile-time-only sanity check, so dropping it costs nothing).
 #define MP_STATIC_ASSERT_NONCONSTEXPR(cond) ((void)1)
 #else
 #if __clang__
@@ -114,9 +116,17 @@ typedef unsigned int uint;
 #else
 #define m_renew(type, ptr, old_num, new_num) ((type *)(m_realloc((ptr), sizeof(type) * (new_num))))
 #define m_renew_maybe(type, ptr, old_num, new_num, allow_move) ((type *)(m_realloc_maybe((ptr), sizeof(type) * (new_num), (allow_move))))
+#if defined(__VBCC__)
+// vbcc errors ("invalid operand type") when casting a multi-term integer
+// expression to void (e.g. (void)(1 + a + 2 * b)); num is a pure size
+// expression with no side effects, so sizeof-discard it to dodge the bug.
+#define m_del(type, ptr, num) ((void)sizeof(num), m_free(ptr))
+#define m_del_var(obj_type, var_field, var_type, var_num, ptr) ((void)sizeof(var_num), m_free(ptr))
+#else
 #define m_del(type, ptr, num) ((void)(num), m_free(ptr))
 #define m_del_var(obj_type, var_field, var_type, var_num, ptr) ((void)(var_num), m_free(ptr))
 #endif
+#endif
 #define m_del_obj(type, ptr) (m_del(type, ptr, 1))
 
 void *m_malloc(size_t num_bytes);
@@ -272,11 +282,13 @@ extern mp_uint_t mp_verbose_flag;
 #define MP_FLOAT_EXP_OFFSET (1023)
 #define MP_FLOAT_FRAC_BITS (52)
 typedef uint64_t mp_float_uint_t;
+typedef int64_t mp_float_int_t;
 #elif MICROPY_FLOAT_IMPL == MICROPY_FLOAT_IMPL_FLOAT
 #define MP_FLOAT_EXP_BITS (8)
 #define MP_FLOAT_EXP_OFFSET (127)
 #define MP_FLOAT_FRAC_BITS (23)
 typedef uint32_t mp_float_uint_t;
+typedef int32_t mp_float_int_t;
 #endif
 
 #define MP_FLOAT_EXP_BIAS ((1 << (MP_FLOAT_EXP_BITS - 1)) - 1)
@@ -299,6 +311,14 @@ typedef union _mp_float_union_t {
     mp_float_uint_t i;
 } mp_float_union_t;
 
+// Bitfield-free accessors for mp_float_union_t. Calypsi 65816 miscompiles
+// read-modify-write of bitfields with 32-bit storage units (and its
+// optimizer can ICE on them), so ports use these shift/mask equivalents.
+#define MP_FLOAT_UNION_SGN(u) ((int)((u).i >> (MP_FLOAT_FRAC_BITS + MP_FLOAT_EXP_BITS)) & 1)
+#define MP_FLOAT_UNION_EXP(u) ((int)((u).i >> MP_FLOAT_FRAC_BITS) & ((1 << MP_FLOAT_EXP_BITS) - 1))
+#define MP_FLOAT_UNION_FRC(u) ((u).i & (((mp_float_uint_t)1 << MP_FLOAT_FRAC_BITS) - 1))
+#define MP_FLOAT_UNION_ADD_EXP(u, d) ((u).i += (mp_float_uint_t)(mp_float_int_t)(d) << MP_FLOAT_FRAC_BITS)
+
 #if MICROPY_FLOAT_FORMAT_IMPL == MICROPY_FLOAT_FORMAT_IMPL_EXACT
 
 #if MICROPY_FLOAT_IMPL == MICROPY_FLOAT_IMPL_FLOAT
@@ -415,6 +435,43 @@ static inline uint32_t mp_ctz(uint32_t x) {
 static inline uint32_t mp_popcount(uint32_t x) {
     return __popcnt(x);
 }
+#elif defined(__VBCC__)
+// vbcc has no __builtin_clz/ctz/popcount; software implementations. mp_int_t
+// is 32-bit (long) here, so mp_clz_mpi resolves to mp_clzl, never mp_clzll —
+// mp_clzll stays a macro (never instantiated) to avoid 64-bit codegen, which
+// vbcc 65816 cannot do at all.
+static inline uint32_t mp_clz(uint32_t x) {
+    uint32_t n = 0;
+    if (x == 0) {
+        return 32;
+    }
+    while (!(x & 0x80000000u)) {
+        x <<= 1;
+        n++;
+    }
+    return n;
+}
+static inline uint32_t mp_clzl(unsigned long x) {
+    return mp_clz((uint32_t)x);
+}
+#define mp_clzll(x) (32u)
+static inline uint32_t mp_ctz(uint32_t x) {
+    uint32_t n = 0;
+    if (x == 0) {
+        return 0;
+    }
+    while (!(x & 1u)) {
+        x >>= 1;
+        n++;
+    }
+    return n;
+}
+static inline uint32_t mp_popcount(uint32_t x) {
+    x = x - ((x >> 1) & 0x55555555);
+    x = (x & 0x33333333) + ((x >> 2) & 0x33333333);
+    x = (x + (x >> 4)) & 0x0F0F0F0F;
+    return (x * 0x01010101) >> 24;
+}
 #else // _MSC_VER
 #define mp_clz(x) __builtin_clz(x)
 #define mp_clzl(x) __builtin_clzl(x)
@@ -481,6 +538,20 @@ static inline bool mp_mul_mp_int_t_overflow(mp_int_t x, mp_int_t y, mp_int_t *re
     return __builtin_mul_overflow(x, y, res);
 }
 
+#elif defined(__VBCC__)
+
+// vbcc 65816 has no 64-bit (long long) runtime — even a bare `long long`
+// multiply fails to link. mp_mul_mp_int_t_overflow is 32-bit (mp_int_t) and is
+// the only one actually used (LONGINT_IMPL is NONE). The unsigned-long-long
+// variant is stubbed to emit no 64-bit arithmetic; it is never called here.
+bool mp_mul_mp_int_t_overflow(mp_int_t x, mp_int_t y, mp_int_t *res);
+static inline bool mp_mul_ull_overflow(unsigned long long int x, unsigned long long int y, unsigned long long int *res) {
+    (void)x;
+    (void)y;
+    *res = 0;
+    return true;
+}
+
 #else
 
 bool mp_mul_ll_overflow(long long int x, long long int y, long long int *res);
@@ -497,6 +568,14 @@ static inline bool mp_mul_ull_overflow(unsigned long long int x, unsigned long l
 
 #if __has_builtin(__builtin_saddll_overflow) || MP_GCC_HAS_BUILTIN_OVERFLOW
 #define mp_add_ll_overflow __builtin_saddll_overflow
+#elif defined(__VBCC__)
+// Unused (LONGINT_IMPL NONE); stubbed to emit no 64-bit arithmetic (no vbcc rt).
+static inline bool mp_add_ll_overflow(long long int lhs, long long int rhs, long long int *res) {
+    (void)lhs;
+    (void)rhs;
+    *res = 0;
+    return true;
+}
 #else
 static inline bool mp_add_ll_overflow(long long int lhs, long long int rhs, long long int *res) {
     bool overflow;
@@ -517,6 +596,14 @@ static inline bool mp_add_ll_overflow(long long int lhs, long long int rhs, long
 
 #if __has_builtin(__builtin_ssubll_overflow) || MP_GCC_HAS_BUILTIN_OVERFLOW
 #define mp_sub_ll_overflow __builtin_ssubll_overflow
+#elif defined(__VBCC__)
+// Unused (LONGINT_IMPL NONE); stubbed to emit no 64-bit arithmetic (no vbcc rt).
+static inline bool mp_sub_ll_overflow(long long int lhs, long long int rhs, long long int *res) {
+    (void)lhs;
+    (void)rhs;
+    *res = 0;
+    return true;
+}
 #else
 static inline bool mp_sub_ll_overflow(long long int lhs, long long int rhs, long long int *res) {
     bool overflow;
diff --git a/py/modarray.c b/py/modarray.c
index 116c844e8..1fe9288be 100644
--- a/py/modarray.c
+++ b/py/modarray.c
@@ -35,7 +35,7 @@ static const mp_rom_map_elem_t mp_module_array_globals_table[] = {
 
 static MP_DEFINE_CONST_DICT(mp_module_array_globals, mp_module_array_globals_table);
 
-const mp_obj_module_t mp_module_array = {
+const mp_obj_module_t MICROPY_OBJ_BASE_ALIGNMENT mp_module_array = {
     .base = { &mp_type_module },
     .globals = (mp_obj_dict_t *)&mp_module_array_globals,
 };
diff --git a/py/modbuiltins.c b/py/modbuiltins.c
index 6f085f2df..5fc2e9ff4 100644
--- a/py/modbuiltins.c
+++ b/py/modbuiltins.c
@@ -96,8 +96,13 @@ MP_DEFINE_CONST_FUN_OBJ_1(mp_builtin_abs_obj, mp_builtin_abs);
 static mp_obj_t mp_builtin_all(mp_obj_t o_in) {
     mp_obj_iter_buf_t iter_buf;
     mp_obj_t iterable = mp_getiter(o_in, &iter_buf);
+    // mp_store_obj_result: Calypsi ora-clobber workaround (DECISIONS.md)
     mp_obj_t item;
-    while ((item = mp_iternext(iterable)) != MP_OBJ_STOP_ITERATION) {
+    for (;;) {
+        mp_store_obj_result(mp_iternext(iterable), &item);
+        if (item == MP_OBJ_STOP_ITERATION) {
+            break;
+        }
         if (!mp_obj_is_true(item)) {
             return mp_const_false;
         }
@@ -109,8 +114,13 @@ MP_DEFINE_CONST_FUN_OBJ_1(mp_builtin_all_obj, mp_builtin_all);
 static mp_obj_t mp_builtin_any(mp_obj_t o_in) {
     mp_obj_iter_buf_t iter_buf;
     mp_obj_t iterable = mp_getiter(o_in, &iter_buf);
+    // mp_store_obj_result: Calypsi ora-clobber workaround (DECISIONS.md)
     mp_obj_t item;
-    while ((item = mp_iternext(iterable)) != MP_OBJ_STOP_ITERATION) {
+    for (;;) {
+        mp_store_obj_result(mp_iternext(iterable), &item);
+        if (item == MP_OBJ_STOP_ITERATION) {
+            break;
+        }
         if (mp_obj_is_true(item)) {
             return mp_const_true;
         }
@@ -517,8 +527,13 @@ static mp_obj_t mp_builtin_sum(size_t n_args, const mp_obj_t *args) {
     }
     mp_obj_iter_buf_t iter_buf;
     mp_obj_t iterable = mp_getiter(args[0], &iter_buf);
+    // mp_store_obj_result: Calypsi ora-clobber workaround (DECISIONS.md)
     mp_obj_t item;
-    while ((item = mp_iternext(iterable)) != MP_OBJ_STOP_ITERATION) {
+    for (;;) {
+        mp_store_obj_result(mp_iternext(iterable), &item);
+        if (item == MP_OBJ_STOP_ITERATION) {
+            break;
+        }
         value = mp_binary_op(MP_BINARY_OP_ADD, value, item);
     }
     return value;
@@ -771,7 +786,7 @@ static const mp_rom_map_elem_t mp_module_builtins_globals_table[] = {
 
 MP_DEFINE_CONST_DICT(mp_module_builtins_globals, mp_module_builtins_globals_table);
 
-const mp_obj_module_t mp_module_builtins = {
+const mp_obj_module_t MICROPY_OBJ_BASE_ALIGNMENT mp_module_builtins = {
     .base = { &mp_type_module },
     .globals = (mp_obj_dict_t *)&mp_module_builtins_globals,
 };
diff --git a/py/modcmath.c b/py/modcmath.c
index 33cb00cbe..62b484aa3 100644
--- a/py/modcmath.c
+++ b/py/modcmath.c
@@ -144,7 +144,7 @@ static const mp_rom_map_elem_t mp_module_cmath_globals_table[] = {
 
 static MP_DEFINE_CONST_DICT(mp_module_cmath_globals, mp_module_cmath_globals_table);
 
-const mp_obj_module_t mp_module_cmath = {
+const mp_obj_module_t MICROPY_OBJ_BASE_ALIGNMENT mp_module_cmath = {
     .base = { &mp_type_module },
     .globals = (mp_obj_dict_t *)&mp_module_cmath_globals,
 };
diff --git a/py/modcollections.c b/py/modcollections.c
index 46326d13e..623cb7284 100644
--- a/py/modcollections.c
+++ b/py/modcollections.c
@@ -41,7 +41,7 @@ static const mp_rom_map_elem_t mp_module_collections_globals_table[] = {
 
 static MP_DEFINE_CONST_DICT(mp_module_collections_globals, mp_module_collections_globals_table);
 
-const mp_obj_module_t mp_module_collections = {
+const mp_obj_module_t MICROPY_OBJ_BASE_ALIGNMENT mp_module_collections = {
     .base = { &mp_type_module },
     .globals = (mp_obj_dict_t *)&mp_module_collections_globals,
 };
diff --git a/py/moderrno.c b/py/moderrno.c
index 58a141c10..d7a6ee4ce 100644
--- a/py/moderrno.c
+++ b/py/moderrno.c
@@ -94,7 +94,7 @@ static const mp_rom_map_elem_t mp_module_errno_globals_table[] = {
 
 static MP_DEFINE_CONST_DICT(mp_module_errno_globals, mp_module_errno_globals_table);
 
-const mp_obj_module_t mp_module_errno = {
+const mp_obj_module_t MICROPY_OBJ_BASE_ALIGNMENT mp_module_errno = {
     .base = { &mp_type_module },
     .globals = (mp_obj_dict_t *)&mp_module_errno_globals,
 };
diff --git a/py/modgc.c b/py/modgc.c
index 47902d8c9..99c4a2285 100644
--- a/py/modgc.c
+++ b/py/modgc.c
@@ -115,7 +115,7 @@ static const mp_rom_map_elem_t mp_module_gc_globals_table[] = {
 
 static MP_DEFINE_CONST_DICT(mp_module_gc_globals, mp_module_gc_globals_table);
 
-const mp_obj_module_t mp_module_gc = {
+const mp_obj_module_t MICROPY_OBJ_BASE_ALIGNMENT mp_module_gc = {
     .base = { &mp_type_module },
     .globals = (mp_obj_dict_t *)&mp_module_gc_globals,
 };
diff --git a/py/modio.c b/py/modio.c
index 9aeb42d30..8444f384c 100644
--- a/py/modio.c
+++ b/py/modio.c
@@ -222,7 +222,7 @@ static const mp_rom_map_elem_t mp_module_io_globals_table[] = {
 
 static MP_DEFINE_CONST_DICT(mp_module_io_globals, mp_module_io_globals_table);
 
-const mp_obj_module_t mp_module_io = {
+const mp_obj_module_t MICROPY_OBJ_BASE_ALIGNMENT mp_module_io = {
     .base = { &mp_type_module },
     .globals = (mp_obj_dict_t *)&mp_module_io_globals,
 };
diff --git a/py/modmath.c b/py/modmath.c
index 045c84215..124e0e691 100644
--- a/py/modmath.c
+++ b/py/modmath.c
@@ -449,7 +449,7 @@ static const mp_rom_map_elem_t mp_module_math_globals_table[] = {
 
 static MP_DEFINE_CONST_DICT(mp_module_math_globals, mp_module_math_globals_table);
 
-const mp_obj_module_t mp_module_math = {
+const mp_obj_module_t MICROPY_OBJ_BASE_ALIGNMENT mp_module_math = {
     .base = { &mp_type_module },
     .globals = (mp_obj_dict_t *)&mp_module_math_globals,
 };
diff --git a/py/modmicropython.c b/py/modmicropython.c
index 4d676cb4a..3fb469eb8 100644
--- a/py/modmicropython.c
+++ b/py/modmicropython.c
@@ -210,7 +210,7 @@ static const mp_rom_map_elem_t mp_module_micropython_globals_table[] = {
 
 static MP_DEFINE_CONST_DICT(mp_module_micropython_globals, mp_module_micropython_globals_table);
 
-const mp_obj_module_t mp_module_micropython = {
+const mp_obj_module_t MICROPY_OBJ_BASE_ALIGNMENT mp_module_micropython = {
     .base = { &mp_type_module },
     .globals = (mp_obj_dict_t *)&mp_module_micropython_globals,
 };
diff --git a/py/modstring.c b/py/modstring.c
index 26e0c2d83..12b562f0c 100644
--- a/py/modstring.c
+++ b/py/modstring.c
@@ -46,7 +46,7 @@ static const mp_rom_map_elem_t mp_module_string_globals_table[] = {
 };
 static MP_DEFINE_CONST_DICT(mp_module_string_globals, mp_module_string_globals_table);
 
-const mp_obj_module_t mp_module_string = {
+const mp_obj_module_t MICROPY_OBJ_BASE_ALIGNMENT mp_module_string = {
     .base = { &mp_type_module },
     .globals = (mp_obj_dict_t *)&mp_module_string_globals,
 };
diff --git a/py/modstruct.c b/py/modstruct.c
index 3b9dd30aa..0bf0dd259 100644
--- a/py/modstruct.c
+++ b/py/modstruct.c
@@ -268,7 +268,7 @@ static const mp_rom_map_elem_t mp_module_struct_globals_table[] = {
 
 static MP_DEFINE_CONST_DICT(mp_module_struct_globals, mp_module_struct_globals_table);
 
-const mp_obj_module_t mp_module_struct = {
+const mp_obj_module_t MICROPY_OBJ_BASE_ALIGNMENT mp_module_struct = {
     .base = { &mp_type_module },
     .globals = (mp_obj_dict_t *)&mp_module_struct_globals,
 };
diff --git a/py/modsys.c b/py/modsys.c
index 3dae92681..9b4b589e8 100644
--- a/py/modsys.c
+++ b/py/modsys.c
@@ -60,11 +60,11 @@ static const MP_DEFINE_STR_OBJ(mp_sys_version_obj, "3.4.0; " MICROPY_BANNER_NAME
 
 // version_info - Python language version that this implementation conforms to, as a tuple of ints
 // TODO: CPython is now at 5-element array (major, minor, micro, releaselevel, serial), but save 2 els so far...
-static const mp_rom_obj_tuple_t mp_sys_version_info_obj = {{&mp_type_tuple}, 3, {MP_ROM_INT(3), MP_ROM_INT(4), MP_ROM_INT(0)}};
+static const mp_rom_obj_tuple_t MICROPY_OBJ_BASE_ALIGNMENT mp_sys_version_info_obj = {{&mp_type_tuple}, 3, {MP_ROM_INT(3), MP_ROM_INT(4), MP_ROM_INT(0)}};
 
 // sys.implementation object
 // this holds the MicroPython version
-static const mp_rom_obj_tuple_t mp_sys_implementation_version_info_obj = {
+static const mp_rom_obj_tuple_t MICROPY_OBJ_BASE_ALIGNMENT mp_sys_implementation_version_info_obj = {
     {&mp_type_tuple},
     4,
     {
@@ -158,7 +158,7 @@ static MP_DEFINE_ATTRTUPLE(
     SYS_IMPLEMENTATION_ELEMS__V2
     );
 #else
-static const mp_rom_obj_tuple_t mp_sys_implementation_obj = {
+static const mp_rom_obj_tuple_t MICROPY_OBJ_BASE_ALIGNMENT mp_sys_implementation_obj = {
     {&mp_type_tuple},
     3 + MICROPY_PERSISTENT_CODE_LOAD,
     // Do not include SYS_IMPLEMENTATION_ELEMS__BUILD, SYS_IMPLEMENTATION_ELEMS__THREAD
@@ -380,7 +380,7 @@ static const mp_rom_map_elem_t mp_module_sys_globals_table[] = {
 
 static MP_DEFINE_CONST_DICT(mp_module_sys_globals, mp_module_sys_globals_table);
 
-const mp_obj_module_t mp_module_sys = {
+const mp_obj_module_t MICROPY_OBJ_BASE_ALIGNMENT mp_module_sys = {
     .base = { &mp_type_module },
     .globals = (mp_obj_dict_t *)&mp_module_sys_globals,
 };
diff --git a/py/modthread.c b/py/modthread.c
index 7742ff684..378d30475 100644
--- a/py/modthread.c
+++ b/py/modthread.c
@@ -281,7 +281,7 @@ static const mp_rom_map_elem_t mp_module_thread_globals_table[] = {
 
 static MP_DEFINE_CONST_DICT(mp_module_thread_globals, mp_module_thread_globals_table);
 
-const mp_obj_module_t mp_module_thread = {
+const mp_obj_module_t MICROPY_OBJ_BASE_ALIGNMENT mp_module_thread = {
     .base = { &mp_type_module },
     .globals = (mp_obj_dict_t *)&mp_module_thread_globals,
 };
diff --git a/py/modweakref.c b/py/modweakref.c
index 3360a4e20..ca327a998 100644
--- a/py/modweakref.c
+++ b/py/modweakref.c
@@ -303,7 +303,7 @@ static const mp_rom_map_elem_t mp_module_weakref_globals_table[] = {
 };
 static MP_DEFINE_CONST_DICT(mp_module_weakref_globals, mp_module_weakref_globals_table);
 
-const mp_obj_module_t mp_module_weakref = {
+const mp_obj_module_t MICROPY_OBJ_BASE_ALIGNMENT mp_module_weakref = {
     .base = { &mp_type_module },
     .globals = (mp_obj_dict_t *)&mp_module_weakref_globals,
 };
diff --git a/py/mpconfig.h b/py/mpconfig.h
index 8d28775c8..0519fa26f 100644
--- a/py/mpconfig.h
+++ b/py/mpconfig.h
@@ -261,6 +261,13 @@ typedef uint64_t mp_uint_t;
 // be reduced to save memory when the heap is small enough.  The type must be
 // big enough to index all blocks in the heap, which is set by
 // heap-size-in-bytes / MICROPY_BYTES_PER_GC_BLOCK.
+// Whether the mp_state root-pointer section can contain pointer fields at
+// non-pointer-aligned offsets (compilers that pack structs and have a
+// size_t narrower than a pointer); adds a phase-shifted second root scan.
+#ifndef MICROPY_GC_UNALIGNED_ROOT_SECTION
+#define MICROPY_GC_UNALIGNED_ROOT_SECTION (0)
+#endif
+
 #ifndef MICROPY_GC_STACK_ENTRY_TYPE
 #define MICROPY_GC_STACK_ENTRY_TYPE size_t
 #endif
diff --git a/py/mpz.c b/py/mpz.c
index 5a4d7d27d..82c694a9f 100644
--- a/py/mpz.c
+++ b/py/mpz.c
@@ -758,16 +758,16 @@ void mpz_set_from_ll(mpz_t *z, long long val, bool is_signed) {
 #if MICROPY_PY_BUILTINS_FLOAT
 void mpz_set_from_float(mpz_t *z, mp_float_t src) {
     mp_float_union_t u = {src};
-    z->neg = u.p.sgn;
-    if (u.p.exp == 0) {
+    z->neg = MP_FLOAT_UNION_SGN(u);
+    if (MP_FLOAT_UNION_EXP(u) == 0) {
         // value == 0 || value < 1
         mpz_set_from_int(z, 0);
-    } else if (u.p.exp == ((1 << MP_FLOAT_EXP_BITS) - 1)) {
-        // u.p.frc == 0 indicates inf, else NaN
+    } else if (MP_FLOAT_UNION_EXP(u) == ((1 << MP_FLOAT_EXP_BITS) - 1)) {
+        // frc == 0 indicates inf, else NaN
         // should be handled by caller
         mpz_set_from_int(z, 0);
     } else {
-        const int adj_exp = (int)u.p.exp - MP_FLOAT_EXP_BIAS;
+        const int adj_exp = MP_FLOAT_UNION_EXP(u) - MP_FLOAT_EXP_BIAS;
         if (adj_exp < 0) {
             // value < 1 , truncates to 0
             mpz_set_from_int(z, 0);
@@ -779,7 +779,7 @@ void mpz_set_from_float(mpz_t *z, mp_float_t src) {
             const int dig_cnt = (adj_exp + 1 + (DIG_SIZE - 1)) / DIG_SIZE;
             const unsigned int rem = adj_exp % DIG_SIZE;
             int dig_ind, shft;
-            mp_float_uint_t frc = u.p.frc | ((mp_float_uint_t)1 << MP_FLOAT_FRAC_BITS);
+            mp_float_uint_t frc = MP_FLOAT_UNION_FRC(u) | ((mp_float_uint_t)1 << MP_FLOAT_FRAC_BITS);
 
             if (adj_exp < MP_FLOAT_FRAC_BITS) {
                 shft = 0;
diff --git a/py/obj.c b/py/obj.c
index 26a912fc6..6f540fd29 100644
--- a/py/obj.c
+++ b/py/obj.c
@@ -37,6 +37,19 @@
 #include "py/cstack.h"
 #include "py/stream.h" // for mp_obj_print
 
+// Calypsi 65816 workaround (DECISIONS.md): the compiler sometimes tests a
+// 32-bit call result against zero by OR-ing its halves in the accumulator
+// and then stores the OR-clobbered accumulator as the value's low word
+// ("assignment inside condition" shape). These store helpers move the
+// store into a separate call, so the caller tests a clean memory copy.
+// Called through volatile function pointers because Calypsi also ignores
+// noinline (inlining would reintroduce the fused test).
+static void mp_store_obj_result_impl(mp_obj_t val, mp_obj_t *out) {
+    *out = val;
+}
+void (*volatile mp_store_obj_result)(mp_obj_t, mp_obj_t *) = mp_store_obj_result_impl;
+
+
 // Allocates an object and also sets type, for mp_obj_malloc{,_var} macros.
 MP_NOINLINE void *mp_obj_malloc_helper(size_t num_bytes, const mp_obj_type_t *type) {
     mp_obj_base_t *base = (mp_obj_base_t *)m_malloc(num_bytes);
@@ -180,7 +193,8 @@ bool mp_obj_is_true(mp_obj_t arg) {
     } else {
         const mp_obj_type_t *type = mp_obj_get_type(arg);
         if (MP_OBJ_TYPE_HAS_SLOT(type, unary_op)) {
-            mp_obj_t result = MP_OBJ_TYPE_GET_SLOT(type, unary_op)(MP_UNARY_OP_BOOL, arg);
+            mp_obj_t result; // via store helper: Calypsi ora-clobber (DECISIONS.md)
+            mp_store_obj_result(MP_OBJ_TYPE_GET_SLOT(type, unary_op)(MP_UNARY_OP_BOOL, arg), &result);
             if (result != MP_OBJ_NULL) {
                 return result == mp_const_true;
             }
@@ -616,9 +630,15 @@ MP_DEFINE_CONST_FUN_OBJ_1(mp_identity_obj, mp_identity);
 
 bool mp_get_buffer(mp_obj_t obj, mp_buffer_info_t *bufinfo, mp_uint_t flags) {
     const mp_obj_type_t *type = mp_obj_get_type(obj);
-    if (MP_OBJ_TYPE_HAS_SLOT(type, buffer)
-        && MP_OBJ_TYPE_GET_SLOT(type, buffer)(obj, bufinfo, flags & MP_BUFFER_RW) == 0) {
-        return true;
+    if (MP_OBJ_TYPE_HAS_SLOT(type, buffer)) {
+        // result laundered through the store helper: Calypsi fuses the ==0
+        // test into the accumulator and can store the clobbered value
+        // (DECISIONS.md ora-clobber)
+        mp_obj_t res;
+        mp_store_obj_result((mp_obj_t)(uintptr_t)MP_OBJ_TYPE_GET_SLOT(type, buffer)(obj, bufinfo, flags & MP_BUFFER_RW), &res);
+        if (res == (mp_obj_t)(uintptr_t)0) {
+            return true;
+        }
     }
     if (flags & MP_BUFFER_RAISE_IF_UNSUPPORTED) {
         mp_raise_TypeError(MP_ERROR_TEXT("object with buffer protocol required"));
diff --git a/py/obj.h b/py/obj.h
index 3c9122a69..98fed65f1 100644
--- a/py/obj.h
+++ b/py/obj.h
@@ -388,25 +388,25 @@ typedef struct _mp_rom_obj_t { mp_const_obj_t o; } mp_rom_obj_t;
 #define MP_OBJ_FUN_MAKE_SIG(n_args_min, n_args_max, takes_kw) ((uint32_t)((((uint32_t)(n_args_min)) << 17) | (((uint32_t)(n_args_max)) << 1) | ((takes_kw) ? 1 : 0)))
 
 #define MP_DEFINE_CONST_FUN_OBJ_0(obj_name, fun_name) \
-    const mp_obj_fun_builtin_fixed_t obj_name = \
+    const mp_obj_fun_builtin_fixed_t MICROPY_OBJ_BASE_ALIGNMENT obj_name = \
     {.base = {.type = &mp_type_fun_builtin_0}, .fun = {._0 = fun_name}}
 #define MP_DEFINE_CONST_FUN_OBJ_1(obj_name, fun_name) \
-    const mp_obj_fun_builtin_fixed_t obj_name = \
+    const mp_obj_fun_builtin_fixed_t MICROPY_OBJ_BASE_ALIGNMENT obj_name = \
     {.base = {.type = &mp_type_fun_builtin_1}, .fun = {._1 = fun_name}}
 #define MP_DEFINE_CONST_FUN_OBJ_2(obj_name, fun_name) \
-    const mp_obj_fun_builtin_fixed_t obj_name = \
+    const mp_obj_fun_builtin_fixed_t MICROPY_OBJ_BASE_ALIGNMENT obj_name = \
     {.base = {.type = &mp_type_fun_builtin_2}, .fun = {._2 = fun_name}}
 #define MP_DEFINE_CONST_FUN_OBJ_3(obj_name, fun_name) \
-    const mp_obj_fun_builtin_fixed_t obj_name = \
+    const mp_obj_fun_builtin_fixed_t MICROPY_OBJ_BASE_ALIGNMENT obj_name = \
     {.base = {.type = &mp_type_fun_builtin_3}, .fun = {._3 = fun_name}}
 #define MP_DEFINE_CONST_FUN_OBJ_VAR(obj_name, n_args_min, fun_name) \
-    const mp_obj_fun_builtin_var_t obj_name = \
+    const mp_obj_fun_builtin_var_t MICROPY_OBJ_BASE_ALIGNMENT obj_name = \
     {.base = {.type = &mp_type_fun_builtin_var}, .sig = MP_OBJ_FUN_MAKE_SIG(n_args_min, MP_OBJ_FUN_ARGS_MAX, false), .fun = {.var = fun_name}}
 #define MP_DEFINE_CONST_FUN_OBJ_VAR_BETWEEN(obj_name, n_args_min, n_args_max, fun_name) \
-    const mp_obj_fun_builtin_var_t obj_name = \
+    const mp_obj_fun_builtin_var_t MICROPY_OBJ_BASE_ALIGNMENT obj_name = \
     {.base = {.type = &mp_type_fun_builtin_var}, .sig = MP_OBJ_FUN_MAKE_SIG(n_args_min, n_args_max, false), .fun = {.var = fun_name}}
 #define MP_DEFINE_CONST_FUN_OBJ_KW(obj_name, n_args_min, fun_name) \
-    const mp_obj_fun_builtin_var_t obj_name = \
+    const mp_obj_fun_builtin_var_t MICROPY_OBJ_BASE_ALIGNMENT obj_name = \
     {.base = {.type = &mp_type_fun_builtin_var}, .sig = MP_OBJ_FUN_MAKE_SIG(n_args_min, MP_OBJ_FUN_ARGS_MAX, true), .fun = {.kw = fun_name}}
 
 // These macros are used to define constant map/dict objects
@@ -423,7 +423,7 @@ typedef struct _mp_rom_obj_t { mp_const_obj_t o; } mp_rom_obj_t;
     }
 
 #define MP_DEFINE_CONST_DICT_WITH_SIZE(dict_name, table_name, n) \
-    const mp_obj_dict_t dict_name = { \
+    const mp_obj_dict_t MICROPY_OBJ_BASE_ALIGNMENT dict_name = { \
         .base = {&mp_type_dict}, \
         .map = { \
             .all_keys_are_qstrs = 1, \
@@ -443,8 +443,8 @@ typedef struct _mp_rom_obj_t { mp_const_obj_t o; } mp_rom_obj_t;
 #define MP_DECLARE_CONST_STATICMETHOD_OBJ(obj_name) extern const mp_rom_obj_static_class_method_t obj_name
 #define MP_DECLARE_CONST_CLASSMETHOD_OBJ(obj_name) extern const mp_rom_obj_static_class_method_t obj_name
 
-#define MP_DEFINE_CONST_STATICMETHOD_OBJ(obj_name, fun_name) const mp_rom_obj_static_class_method_t obj_name = {{&mp_type_staticmethod}, fun_name}
-#define MP_DEFINE_CONST_CLASSMETHOD_OBJ(obj_name, fun_name) const mp_rom_obj_static_class_method_t obj_name = {{&mp_type_classmethod}, fun_name}
+#define MP_DEFINE_CONST_STATICMETHOD_OBJ(obj_name, fun_name) const mp_rom_obj_static_class_method_t MICROPY_OBJ_BASE_ALIGNMENT obj_name = {{&mp_type_staticmethod}, fun_name}
+#define MP_DEFINE_CONST_CLASSMETHOD_OBJ(obj_name, fun_name) const mp_rom_obj_static_class_method_t MICROPY_OBJ_BASE_ALIGNMENT obj_name = {{&mp_type_classmethod}, fun_name}
 
 #ifndef NO_QSTR
 
@@ -713,7 +713,15 @@ struct _mp_obj_type_t {
     // A dict mapping qstrs to objects local methods/constants/etc.
     uint8_t slot_index_locals_dict;
 
+    #if defined(__CALYPSI__) || defined(__VBCC__)
+    // Calypsi 5.17 silently drops initializer data for flexible array
+    // members (every type's slots came out as zeros). A fixed bound makes
+    // these ordinary array initializers; layout of the used prefix is
+    // identical and MP_DEFINE_CONST_OBJ_TYPE never passes more than 12.
+    const void *slots[12];
+    #else
     const void *slots[];
+    #endif
 };
 
 // Non-variable sized versions of mp_obj_type_t to be used as a member
@@ -780,20 +788,20 @@ typedef struct _mp_obj_full_type_t {
 // Do not use these directly, instead use MP_DEFINE_CONST_OBJ_TYPE.
 // Generated with:
 // for i in range(13):
-//     print(f"#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_{i}(_struct_type, _typename, _name, _flags{''.join(f', f{j+1}, v{j+1}' for j in range(i))}) const _struct_type _typename = {{ .base = {{ &mp_type_type }}, .flags = _flags, .name = _name{''.join(f', .slot_index_##f{j+1} = {j+1}' for j in range(i))}{', .slots = { ' + ''.join(f'v{j+1}, ' for j in range(i)) + '}' if i else '' } }}")
-#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_0(_struct_type, _typename, _name, _flags) const _struct_type _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name }
-#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_1(_struct_type, _typename, _name, _flags, f1, v1) const _struct_type _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slots = { v1, } }
-#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_2(_struct_type, _typename, _name, _flags, f1, v1, f2, v2) const _struct_type _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slots = { v1, v2, } }
-#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_3(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3) const _struct_type _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slots = { v1, v2, v3, } }
-#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_4(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4) const _struct_type _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slots = { v1, v2, v3, v4, } }
-#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_5(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4, f5, v5) const _struct_type _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slot_index_##f5 = 5, .slots = { v1, v2, v3, v4, v5, } }
-#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_6(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4, f5, v5, f6, v6) const _struct_type _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slot_index_##f5 = 5, .slot_index_##f6 = 6, .slots = { v1, v2, v3, v4, v5, v6, } }
-#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_7(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4, f5, v5, f6, v6, f7, v7) const _struct_type _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slot_index_##f5 = 5, .slot_index_##f6 = 6, .slot_index_##f7 = 7, .slots = { v1, v2, v3, v4, v5, v6, v7, } }
-#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_8(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4, f5, v5, f6, v6, f7, v7, f8, v8) const _struct_type _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slot_index_##f5 = 5, .slot_index_##f6 = 6, .slot_index_##f7 = 7, .slot_index_##f8 = 8, .slots = { v1, v2, v3, v4, v5, v6, v7, v8, } }
-#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_9(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4, f5, v5, f6, v6, f7, v7, f8, v8, f9, v9) const _struct_type _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slot_index_##f5 = 5, .slot_index_##f6 = 6, .slot_index_##f7 = 7, .slot_index_##f8 = 8, .slot_index_##f9 = 9, .slots = { v1, v2, v3, v4, v5, v6, v7, v8, v9, } }
-#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_10(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4, f5, v5, f6, v6, f7, v7, f8, v8, f9, v9, f10, v10) const _struct_type _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slot_index_##f5 = 5, .slot_index_##f6 = 6, .slot_index_##f7 = 7, .slot_index_##f8 = 8, .slot_index_##f9 = 9, .slot_index_##f10 = 10, .slots = { v1, v2, v3, v4, v5, v6, v7, v8, v9, v10, } }
-#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_11(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4, f5, v5, f6, v6, f7, v7, f8, v8, f9, v9, f10, v10, f11, v11) const _struct_type _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slot_index_##f5 = 5, .slot_index_##f6 = 6, .slot_index_##f7 = 7, .slot_index_##f8 = 8, .slot_index_##f9 = 9, .slot_index_##f10 = 10, .slot_index_##f11 = 11, .slots = { v1, v2, v3, v4, v5, v6, v7, v8, v9, v10, v11, } }
-#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_12(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4, f5, v5, f6, v6, f7, v7, f8, v8, f9, v9, f10, v10, f11, v11, f12, v12) const _struct_type _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slot_index_##f5 = 5, .slot_index_##f6 = 6, .slot_index_##f7 = 7, .slot_index_##f8 = 8, .slot_index_##f9 = 9, .slot_index_##f10 = 10, .slot_index_##f11 = 11, .slot_index_##f12 = 12, .slots = { v1, v2, v3, v4, v5, v6, v7, v8, v9, v10, v11, v12, } }
+//     print(f"#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_{i}(_struct_type, _typename, _name, _flags{''.join(f', f{j+1}, v{j+1}' for j in range(i))}) const _struct_type MICROPY_OBJ_BASE_ALIGNMENT _typename = {{ .base = {{ &mp_type_type }}, .flags = _flags, .name = _name{''.join(f', .slot_index_##f{j+1} = {j+1}' for j in range(i))}{', .slots = { ' + ''.join(f'v{j+1}, ' for j in range(i)) + '}' if i else '' } }}")
+#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_0(_struct_type, _typename, _name, _flags) const _struct_type MICROPY_OBJ_BASE_ALIGNMENT _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name }
+#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_1(_struct_type, _typename, _name, _flags, f1, v1) const _struct_type MICROPY_OBJ_BASE_ALIGNMENT _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slots = { v1, } }
+#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_2(_struct_type, _typename, _name, _flags, f1, v1, f2, v2) const _struct_type MICROPY_OBJ_BASE_ALIGNMENT _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slots = { v1, v2, } }
+#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_3(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3) const _struct_type MICROPY_OBJ_BASE_ALIGNMENT _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slots = { v1, v2, v3, } }
+#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_4(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4) const _struct_type MICROPY_OBJ_BASE_ALIGNMENT _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slots = { v1, v2, v3, v4, } }
+#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_5(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4, f5, v5) const _struct_type MICROPY_OBJ_BASE_ALIGNMENT _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slot_index_##f5 = 5, .slots = { v1, v2, v3, v4, v5, } }
+#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_6(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4, f5, v5, f6, v6) const _struct_type MICROPY_OBJ_BASE_ALIGNMENT _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slot_index_##f5 = 5, .slot_index_##f6 = 6, .slots = { v1, v2, v3, v4, v5, v6, } }
+#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_7(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4, f5, v5, f6, v6, f7, v7) const _struct_type MICROPY_OBJ_BASE_ALIGNMENT _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slot_index_##f5 = 5, .slot_index_##f6 = 6, .slot_index_##f7 = 7, .slots = { v1, v2, v3, v4, v5, v6, v7, } }
+#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_8(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4, f5, v5, f6, v6, f7, v7, f8, v8) const _struct_type MICROPY_OBJ_BASE_ALIGNMENT _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slot_index_##f5 = 5, .slot_index_##f6 = 6, .slot_index_##f7 = 7, .slot_index_##f8 = 8, .slots = { v1, v2, v3, v4, v5, v6, v7, v8, } }
+#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_9(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4, f5, v5, f6, v6, f7, v7, f8, v8, f9, v9) const _struct_type MICROPY_OBJ_BASE_ALIGNMENT _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slot_index_##f5 = 5, .slot_index_##f6 = 6, .slot_index_##f7 = 7, .slot_index_##f8 = 8, .slot_index_##f9 = 9, .slots = { v1, v2, v3, v4, v5, v6, v7, v8, v9, } }
+#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_10(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4, f5, v5, f6, v6, f7, v7, f8, v8, f9, v9, f10, v10) const _struct_type MICROPY_OBJ_BASE_ALIGNMENT _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slot_index_##f5 = 5, .slot_index_##f6 = 6, .slot_index_##f7 = 7, .slot_index_##f8 = 8, .slot_index_##f9 = 9, .slot_index_##f10 = 10, .slots = { v1, v2, v3, v4, v5, v6, v7, v8, v9, v10, } }
+#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_11(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4, f5, v5, f6, v6, f7, v7, f8, v8, f9, v9, f10, v10, f11, v11) const _struct_type MICROPY_OBJ_BASE_ALIGNMENT _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slot_index_##f5 = 5, .slot_index_##f6 = 6, .slot_index_##f7 = 7, .slot_index_##f8 = 8, .slot_index_##f9 = 9, .slot_index_##f10 = 10, .slot_index_##f11 = 11, .slots = { v1, v2, v3, v4, v5, v6, v7, v8, v9, v10, v11, } }
+#define MP_DEFINE_CONST_OBJ_TYPE_NARGS_12(_struct_type, _typename, _name, _flags, f1, v1, f2, v2, f3, v3, f4, v4, f5, v5, f6, v6, f7, v7, f8, v8, f9, v9, f10, v10, f11, v11, f12, v12) const _struct_type MICROPY_OBJ_BASE_ALIGNMENT _typename = { .base = { &mp_type_type }, .flags = _flags, .name = _name, .slot_index_##f1 = 1, .slot_index_##f2 = 2, .slot_index_##f3 = 3, .slot_index_##f4 = 4, .slot_index_##f5 = 5, .slot_index_##f6 = 6, .slot_index_##f7 = 7, .slot_index_##f8 = 8, .slot_index_##f9 = 9, .slot_index_##f10 = 10, .slot_index_##f11 = 11, .slot_index_##f12 = 12, .slots = { v1, v2, v3, v4, v5, v6, v7, v8, v9, v10, v11, v12, } }
 
 // Because the mp_obj_type_t instances are in (zero-initialised) ROM, we take
 // slot_index_foo=0 to mean that the slot is unset. This also simplifies checking
@@ -822,7 +830,11 @@ typedef struct _mp_obj_full_type_t {
 // numeric values 12, 11, ... 0 by the number of arguments, such that the
 // 29th argument ends up being the number to use. The _INV values are
 // placeholders because the slot arguments come in pairs.
-#define MP_DEFINE_CONST_OBJ_TYPE(...) MP_DEFINE_CONST_OBJ_TYPE_EXPAND(MP_DEFINE_CONST_OBJ_TYPE_NARGS(__VA_ARGS__, _INV, 12, _INV, 11, _INV, 10, _INV, 9, _INV, 8, _INV, 7, _INV, 6, _INV, 5, _INV, 4, _INV, 3, _INV, 2, _INV, 1, _INV, 0)(mp_obj_type_t, __VA_ARGS__))
+// Trailing _INV sentinel: a type with zero slots passes exactly 29 args to
+// MP_DEFINE_CONST_OBJ_TYPE_NARGS, leaving its `...` empty, which vbcc (and
+// strict C99) reject. The sentinel keeps `...` non-empty without shifting N
+// (still the 29th argument). Harmless to other compilers.
+#define MP_DEFINE_CONST_OBJ_TYPE(...) MP_DEFINE_CONST_OBJ_TYPE_EXPAND(MP_DEFINE_CONST_OBJ_TYPE_NARGS(__VA_ARGS__, _INV, 12, _INV, 11, _INV, 10, _INV, 9, _INV, 8, _INV, 7, _INV, 6, _INV, 5, _INV, 4, _INV, 3, _INV, 2, _INV, 1, _INV, 0, _INV)(mp_obj_type_t, __VA_ARGS__))
 
 // Constant types, globally accessible
 extern const mp_obj_type_t mp_type_type;
@@ -1077,6 +1089,8 @@ bool mp_obj_get_complex_maybe(mp_obj_t self_in, mp_float_t *real, mp_float_t *im
 void mp_obj_get_array(mp_obj_t o, size_t *len, mp_obj_t **items); // *items may point inside a GC block
 void mp_obj_get_array_fixed_n(mp_obj_t o, size_t len, mp_obj_t **items); // *items may point inside a GC block
 size_t mp_get_index(const mp_obj_type_t *type, size_t len, mp_obj_t index, bool is_slice);
+// Calypsi ora-clobber workaround, see py/obj.c
+extern void (*volatile mp_store_obj_result)(mp_obj_t, mp_obj_t *);
 mp_obj_t mp_obj_id(mp_obj_t o_in);
 mp_obj_t mp_obj_len(mp_obj_t o_in);
 mp_obj_t mp_obj_len_maybe(mp_obj_t o_in); // may return MP_OBJ_NULL
diff --git a/py/objarray.c b/py/objarray.c
index 75bc671c1..2caf1cbcd 100644
--- a/py/objarray.c
+++ b/py/objarray.c
@@ -53,7 +53,11 @@
 #if MICROPY_PY_BUILTINS_MEMORYVIEW
 #define TYPECODE_MASK (0x7f)
 #define memview_offset free
-#define memview_offset_max ((1LL << MP_OBJ_ARRAY_FREE_SIZE_BITS) - 1)
+#if defined(__CALYPSI__)
+#define memview_offset_max ((size_t)-1) // free is a plain size_t (see objarray.h)
+#else
+#define memview_offset_max (((size_t)1 << (8 * sizeof(size_t) - 8)) - 1)
+#endif
 #else
 // make (& TYPECODE_MASK) a null operation if memorview not enabled
 #define TYPECODE_MASK (~(size_t)0)
diff --git a/py/objarray.h b/py/objarray.h
index bb7a514b9..008e27ec7 100644
--- a/py/objarray.h
+++ b/py/objarray.h
@@ -40,16 +40,37 @@
 // see comment at the beginning of objarray.c.
 typedef struct _mp_obj_array_t {
     mp_obj_base_t base;
+    #if defined(__CALYPSI__)
+    // Calypsi layout, three constraints at once:
+    //  - no bitfields (Calypsi miscompiles bitfield RMW, and with 16-bit
+    //    size_t the upstream 'free' bitfield would only get 8 bits);
+    //  - 'len' and 'items' must sit at the same offsets as mp_obj_str_t's
+    //    'len' and 'data' (generic code aliases the two structs; enforced
+    //    by a static assert in py/runtime.c) -> typecode+pad fill the
+    //    2-byte hash slot and 'free' moves to the end;
+    //  - 'items' must sit at an even offset: the GC heap scan visits only
+    //    even offsets, and with items at offset 9 every bytearray whose
+    //    backing store had no other reference was collected while alive
+    //    (SSD.lut read back as recycled qstr text).
+    // 'free' is the number of unused elements after len used elements
+    // (alloc size = len + free); for memoryview it's reused as the offset
+    // in elements into the parent object. See comments in objarray.c.
+    uint8_t typecode;
+    uint8_t pad_hash_slot;
+    size_t len; // in elements
+    void *items;
+    size_t free;
+    #else
+    // Upstream layout (bitfields keep len/items aligned with mp_obj_str_t)
     size_t typecode : 8;
     // free is number of unused elements after len used elements
     // alloc size = len + free
-    // But for memoryview, 'free' is reused as offset (in elements) into the
-    // parent object. (Union is not used to not go into a complication of
-    // union-of-bitfields with different toolchains). See comments in
-    // objarray.c.
-    size_t free : MP_OBJ_ARRAY_FREE_SIZE_BITS;
+    // For memoryview, 'free' is reused as offset (in elements) into the
+    // parent object. See comments in objarray.c.
+    size_t free : (8 * sizeof(size_t) - 8);
     size_t len; // in elements
     void *items;
+    #endif
 } mp_obj_array_t;
 
 #if MICROPY_PY_BUILTINS_MEMORYVIEW
diff --git a/py/objbool.c b/py/objbool.c
index 5b3e3660e..f460d763b 100644
--- a/py/objbool.c
+++ b/py/objbool.c
@@ -96,6 +96,6 @@ MP_DEFINE_CONST_OBJ_TYPE(
     );
 
 #if !MICROPY_OBJ_IMMEDIATE_OBJS
-const mp_obj_bool_t mp_const_false_obj = {{&mp_type_bool}, false};
-const mp_obj_bool_t mp_const_true_obj = {{&mp_type_bool}, true};
+const mp_obj_bool_t MICROPY_OBJ_BASE_ALIGNMENT mp_const_false_obj = {{&mp_type_bool}, false};
+const mp_obj_bool_t MICROPY_OBJ_BASE_ALIGNMENT mp_const_true_obj = {{&mp_type_bool}, true};
 #endif
diff --git a/py/objclosure.c b/py/objclosure.c
index 3ba507b95..0475d8bb0 100644
--- a/py/objclosure.c
+++ b/py/objclosure.c
@@ -53,6 +53,22 @@ static mp_obj_t closure_call(mp_obj_t self_in, size_t n_args, size_t n_kw, const
         mp_obj_t *args2 = m_new(mp_obj_t, n_total);
         memcpy(args2, self->closed, self->n_closed * sizeof(mp_obj_t));
         memcpy(args2 + self->n_closed, args, (n_args + 2 * n_kw) * sizeof(mp_obj_t));
+        #ifdef CLOSURE_DEBUG
+        {
+            extern void mb_puts(const char *s);
+            extern void mb_puthex32(unsigned long v);
+            union { mp_obj_t o; unsigned char b[4]; } u;
+            mb_puts("[clo n=");
+            mb_puthex32(self->n_closed);
+            mb_puts(" c0=");
+            u.o = self->closed[0];
+            mb_puthex32(u.b[2]); mb_puthex32(u.b[1]); mb_puthex32(u.b[0]);
+            mb_puts(" a0=");
+            u.o = args2[0];
+            mb_puthex32(u.b[2]); mb_puthex32(u.b[1]); mb_puthex32(u.b[0]);
+            mb_puts("]\n");
+        }
+        #endif
         mp_obj_t res = mp_call_function_n_kw(self->fun, self->n_closed + n_args, n_kw, args2);
         m_del(mp_obj_t, args2, n_total);
         return res;
diff --git a/py/objdict.c b/py/objdict.c
index 692a7de42..7fc4f5e95 100644
--- a/py/objdict.c
+++ b/py/objdict.c
@@ -37,7 +37,7 @@ bool mp_obj_is_dict_or_ordereddict(mp_obj_t o) {
     return mp_obj_is_obj(o) && MP_OBJ_TYPE_GET_SLOT_OR_NULL(((mp_obj_base_t *)MP_OBJ_TO_PTR(o))->type, make_new) == mp_obj_dict_make_new;
 }
 
-const mp_obj_dict_t mp_const_empty_dict_obj = {
+const mp_obj_dict_t MICROPY_OBJ_BASE_ALIGNMENT mp_const_empty_dict_obj = {
     .base = { .type = &mp_type_dict },
     .map = {
         .all_keys_are_qstrs = 0,
diff --git a/py/objfloat.c b/py/objfloat.c
index b0ad70de4..cfb22cda1 100644
--- a/py/objfloat.c
+++ b/py/objfloat.c
@@ -57,15 +57,15 @@ typedef struct _mp_obj_float_t {
     mp_float_t value;
 } mp_obj_float_t;
 
-const mp_obj_float_t mp_const_float_e_obj = {{&mp_type_float}, (mp_float_t)M_E};
-const mp_obj_float_t mp_const_float_pi_obj = {{&mp_type_float}, (mp_float_t)M_PI};
+const mp_obj_float_t MICROPY_OBJ_BASE_ALIGNMENT mp_const_float_e_obj = {{&mp_type_float}, (mp_float_t)M_E};
+const mp_obj_float_t MICROPY_OBJ_BASE_ALIGNMENT mp_const_float_pi_obj = {{&mp_type_float}, (mp_float_t)M_PI};
 #if MICROPY_PY_MATH_CONSTANTS
 #ifndef NAN
 #error NAN macro is not defined
 #endif
-const mp_obj_float_t mp_const_float_tau_obj = {{&mp_type_float}, (mp_float_t)(2.0 * M_PI)};
-const mp_obj_float_t mp_const_float_inf_obj = {{&mp_type_float}, (mp_float_t)INFINITY};
-const mp_obj_float_t mp_const_float_nan_obj = {{&mp_type_float}, (mp_float_t)NAN};
+const mp_obj_float_t MICROPY_OBJ_BASE_ALIGNMENT mp_const_float_tau_obj = {{&mp_type_float}, (mp_float_t)(2.0 * M_PI)};
+const mp_obj_float_t MICROPY_OBJ_BASE_ALIGNMENT mp_const_float_inf_obj = {{&mp_type_float}, (mp_float_t)INFINITY};
+const mp_obj_float_t MICROPY_OBJ_BASE_ALIGNMENT mp_const_float_nan_obj = {{&mp_type_float}, (mp_float_t)NAN};
 #endif
 
 #endif
@@ -77,14 +77,14 @@ const mp_obj_float_t mp_const_float_nan_obj = {{&mp_type_float}, (mp_float_t)NAN
 mp_int_t mp_float_hash(mp_float_t src) {
     mp_float_union_t u = {.f = src};
     mp_int_t val;
-    const int adj_exp = (int)u.p.exp - MP_FLOAT_EXP_BIAS;
+    const int adj_exp = MP_FLOAT_UNION_EXP(u) - MP_FLOAT_EXP_BIAS;
     if (adj_exp < 0) {
         // value < 1; must be sure to handle 0.0 correctly (ie return 0)
         val = u.i;
     } else {
         // if adj_exp is max then: u.p.frc==0 indicates inf, else NaN
         // else: 1 <= value
-        mp_float_uint_t frc = u.p.frc | ((mp_float_uint_t)1 << MP_FLOAT_FRAC_BITS);
+        mp_float_uint_t frc = MP_FLOAT_UNION_FRC(u) | ((mp_float_uint_t)1 << MP_FLOAT_FRAC_BITS);
 
         if (adj_exp <= MP_FLOAT_FRAC_BITS) {
             // number may have a fraction; xor the integer part with the fractional part
@@ -99,7 +99,7 @@ mp_int_t mp_float_hash(mp_float_t src) {
         }
     }
 
-    if (u.p.sgn) {
+    if (MP_FLOAT_UNION_SGN(u)) {
         val = -(mp_uint_t)val;
     }
 
diff --git a/py/objgenerator.c b/py/objgenerator.c
index a0cee5d48..895bbb590 100644
--- a/py/objgenerator.c
+++ b/py/objgenerator.c
@@ -36,7 +36,7 @@
 #include "py/cstack.h"
 
 // Instance of GeneratorExit exception - needed by generator.close()
-const mp_obj_exception_t mp_const_GeneratorExit_obj = {{&mp_type_GeneratorExit}, 0, 0, NULL, (mp_obj_tuple_t *)&mp_const_empty_tuple_obj};
+const mp_obj_exception_t MICROPY_OBJ_BASE_ALIGNMENT mp_const_GeneratorExit_obj = {{&mp_type_GeneratorExit}, 0, 0, NULL, (mp_obj_tuple_t *)&mp_const_empty_tuple_obj};
 
 /******************************************************************************/
 /* generator wrapper                                                          */
diff --git a/py/objint.c b/py/objint.c
index 901264aca..fbde0e356 100644
--- a/py/objint.c
+++ b/py/objint.c
@@ -143,9 +143,9 @@ static mp_fp_as_int_class_t mp_classify_fp_as_int(mp_float_t val) {
 mp_obj_t mp_obj_new_int_from_float(mp_float_t val) {
     mp_float_union_t u = {val};
     // IEEE-754: if biased exponent is all 1 bits...
-    if (u.p.exp == ((1 << MP_FLOAT_EXP_BITS) - 1)) {
+    if (MP_FLOAT_UNION_EXP(u) == ((1 << MP_FLOAT_EXP_BITS) - 1)) {
         // ...then number is Inf (positive or negative) if fraction is 0, else NaN.
-        if (u.p.frc == 0) {
+        if (MP_FLOAT_UNION_FRC(u) == 0) {
             mp_raise_msg(&mp_type_OverflowError, MP_ERROR_TEXT("can't convert inf to int"));
         } else {
             mp_raise_ValueError(MP_ERROR_TEXT("can't convert NaN to int"));
diff --git a/py/objlist.c b/py/objlist.c
index 41c920511..08429a386 100644
--- a/py/objlist.c
+++ b/py/objlist.c
@@ -64,8 +64,13 @@ static void list_print(const mp_print_t *print, mp_obj_t o_in, mp_print_kind_t k
 
 static mp_obj_t list_extend_from_iter(mp_obj_t list, mp_obj_t iterable) {
     mp_obj_t iter = mp_getiter(iterable, NULL);
+    // mp_store_obj_result: Calypsi ora-clobber workaround (DECISIONS.md)
     mp_obj_t item;
-    while ((item = mp_iternext(iter)) != MP_OBJ_STOP_ITERATION) {
+    for (;;) {
+        mp_store_obj_result(mp_iternext(iter), &item);
+        if (item == MP_OBJ_STOP_ITERATION) {
+            break;
+        }
         mp_obj_list_append(list, item);
     }
     return list;
@@ -511,7 +516,7 @@ static mp_obj_t list_it_iternext(mp_obj_t self_in) {
     }
 }
 
-mp_obj_t mp_obj_new_list_iterator(mp_obj_t list, size_t cur, mp_obj_iter_buf_t *iter_buf) {
+static mp_obj_t mp_obj_new_list_iterator(mp_obj_t list, size_t cur, mp_obj_iter_buf_t *iter_buf) {
     assert(sizeof(mp_obj_list_it_t) <= sizeof(mp_obj_iter_buf_t));
     mp_obj_list_it_t *o = (mp_obj_list_it_t *)iter_buf;
     o->base.type = &mp_type_polymorph_iter;
diff --git a/py/objmap.c b/py/objmap.c
index 1911a7510..c18c2570d 100644
--- a/py/objmap.c
+++ b/py/objmap.c
@@ -31,8 +31,12 @@
 
 typedef struct _mp_obj_map_t {
     mp_obj_base_t base;
-    size_t n_iters;
+    // fun before n_iters: with 16-bit size_t and packed structs the fun
+    // pointer would sit at offset 6, and Calypsi has repeatedly miscompiled
+    // 32-bit accesses to 2-mod-4 struct offsets (see DECISIONS.md). Keeping
+    // pointer fields 4-aligned dodges the codegen roulette.
     mp_obj_t fun;
+    size_t n_iters;
     mp_obj_t iters[];
 } mp_obj_map_t;
 
@@ -41,6 +45,20 @@ static mp_obj_t map_make_new(const mp_obj_type_t *type, size_t n_args, size_t n_
     mp_obj_map_t *o = mp_obj_malloc_var(mp_obj_map_t, iters, mp_obj_t, n_args - 1, type);
     o->n_iters = n_args - 1;
     o->fun = args[0];
+    #ifdef MAP_DEBUG
+    {
+        extern void mb_puts(const char *s);
+        extern void mb_puthex32(unsigned long v);
+        union { mp_obj_t o; unsigned char b[4]; } u;
+        mb_puts("[mk arg0=");
+        u.o = args[0];
+        mb_puthex32(u.b[2]); mb_puthex32(u.b[1]); mb_puthex32(u.b[0]);
+        mb_puts(" fun=");
+        u.o = o->fun;
+        mb_puthex32(u.b[2]); mb_puthex32(u.b[1]); mb_puthex32(u.b[0]);
+        mb_puts("]\n");
+    }
+    #endif
     for (size_t i = 0; i < n_args - 1; i++) {
         o->iters[i] = mp_getiter(args[i + 1], NULL);
     }
@@ -60,6 +78,19 @@ static mp_obj_t map_iternext(mp_obj_t self_in) {
         }
         nextses[i] = next;
     }
+    #ifdef MAP_DEBUG
+    {
+        extern void mb_puts(const char *s);
+        extern void mb_puthex32(unsigned long v);
+        union { mp_obj_t o; unsigned char b[4]; } u;
+        mp_obj_t res = mp_call_function_n_kw(self->fun, self->n_iters, 0, nextses);
+        mb_puts("[it got=");
+        u.o = res;
+        mb_puthex32(u.b[2]); mb_puthex32(u.b[1]); mb_puthex32(u.b[0]);
+        mb_puts("]\n");
+        return res;
+    }
+    #endif
     return mp_call_function_n_kw(self->fun, self->n_iters, 0, nextses);
 }
 
diff --git a/py/objmodule.c b/py/objmodule.c
index 2ffa7847f..9470e4183 100644
--- a/py/objmodule.c
+++ b/py/objmodule.c
@@ -47,7 +47,8 @@ static void module_print(const mp_print_t *print, mp_obj_t self_in, mp_print_kin
     #if MICROPY_MODULE___FILE__
     // If we store __file__ to imported modules then try to lookup this
     // symbol to give more information about the module.
-    elem = mp_map_lookup(&self->globals->map, MP_OBJ_NEW_QSTR(MP_QSTR___file__), MP_MAP_LOOKUP);
+    // via store helper: Calypsi ora-clobber workaround (DECISIONS.md)
+    mp_store_obj_result((mp_obj_t)mp_map_lookup(&self->globals->map, MP_OBJ_NEW_QSTR(MP_QSTR___file__), MP_MAP_LOOKUP), (mp_obj_t *)&elem);
     if (elem != NULL) {
         mp_printf(print, "<module '%s' from '%s'>", module_name, mp_obj_str_get_str(elem->value));
         return;
diff --git a/py/objnone.c b/py/objnone.c
index a8ce8ebfe..635f41137 100644
--- a/py/objnone.c
+++ b/py/objnone.c
@@ -51,5 +51,5 @@ MP_DEFINE_CONST_OBJ_TYPE(
     );
 
 #if !MICROPY_OBJ_IMMEDIATE_OBJS
-const mp_obj_none_t mp_const_none_obj = {{&mp_type_NoneType}};
+const mp_obj_none_t MICROPY_OBJ_BASE_ALIGNMENT mp_const_none_obj = {{&mp_type_NoneType}};
 #endif
diff --git a/py/objset.c b/py/objset.c
index c8fa12a7e..daa8577f2 100644
--- a/py/objset.c
+++ b/py/objset.c
@@ -206,8 +206,12 @@ static mp_obj_t set_diff_int(size_t n_args, const mp_obj_t *args, bool update) {
         } else {
             mp_set_t *self_set = &((mp_obj_set_t *)MP_OBJ_TO_PTR(self))->set;
             mp_obj_t iter = mp_getiter(other, NULL);
-            mp_obj_t next;
-            while ((next = mp_iternext(iter)) != MP_OBJ_STOP_ITERATION) {
+            mp_obj_t next; // Calypsi ora-clobber workaround (DECISIONS.md)
+            for (;;) {
+                mp_store_obj_result(mp_iternext(iter), &next);
+                if (next == MP_OBJ_STOP_ITERATION) {
+                    break;
+                }
                 mp_set_lookup(self_set, next, MP_MAP_LOOKUP_REMOVE_IF_FOUND);
             }
         }
@@ -242,8 +246,13 @@ static mp_obj_t set_intersect_int(mp_obj_t self_in, mp_obj_t other, bool update)
     mp_obj_set_t *out = MP_OBJ_TO_PTR(mp_obj_new_set(0, NULL));
 
     mp_obj_t iter = mp_getiter(other, NULL);
+    // mp_store_obj_result: Calypsi ora-clobber workaround (DECISIONS.md)
     mp_obj_t next;
-    while ((next = mp_iternext(iter)) != MP_OBJ_STOP_ITERATION) {
+    for (;;) {
+        mp_store_obj_result(mp_iternext(iter), &next);
+        if (next == MP_OBJ_STOP_ITERATION) {
+            break;
+        }
         if (mp_set_lookup(&self->set, next, MP_MAP_LOOKUP)) {
             set_add(MP_OBJ_FROM_PTR(out), next);
         }
@@ -275,8 +284,13 @@ static mp_obj_t set_isdisjoint(mp_obj_t self_in, mp_obj_t other) {
 
     mp_obj_iter_buf_t iter_buf;
     mp_obj_t iter = mp_getiter(other, &iter_buf);
+    // mp_store_obj_result: Calypsi ora-clobber workaround (DECISIONS.md)
     mp_obj_t next;
-    while ((next = mp_iternext(iter)) != MP_OBJ_STOP_ITERATION) {
+    for (;;) {
+        mp_store_obj_result(mp_iternext(iter), &next);
+        if (next == MP_OBJ_STOP_ITERATION) {
+            break;
+        }
         if (mp_set_lookup(&self->set, next, MP_MAP_LOOKUP)) {
             return mp_const_false;
         }
@@ -381,8 +395,13 @@ static mp_obj_t set_symmetric_difference_update(mp_obj_t self_in, mp_obj_t other
     check_set_or_frozenset(self_in); // can be frozenset due to call from set_symmetric_difference
     mp_obj_set_t *self = MP_OBJ_TO_PTR(self_in);
     mp_obj_t iter = mp_getiter(other_in, NULL);
+    // mp_store_obj_result: Calypsi ora-clobber workaround (DECISIONS.md)
     mp_obj_t next;
-    while ((next = mp_iternext(iter)) != MP_OBJ_STOP_ITERATION) {
+    for (;;) {
+        mp_store_obj_result(mp_iternext(iter), &next);
+        if (next == MP_OBJ_STOP_ITERATION) {
+            break;
+        }
         mp_set_lookup(&self->set, next, MP_MAP_LOOKUP_ADD_IF_NOT_FOUND_OR_REMOVE_IF_FOUND);
     }
     return mp_const_none;
@@ -398,8 +417,13 @@ static MP_DEFINE_CONST_FUN_OBJ_2(set_symmetric_difference_obj, set_symmetric_dif
 
 static void set_update_int(mp_obj_set_t *self, mp_obj_t other_in) {
     mp_obj_t iter = mp_getiter(other_in, NULL);
+    // mp_store_obj_result: Calypsi ora-clobber workaround (DECISIONS.md)
     mp_obj_t next;
-    while ((next = mp_iternext(iter)) != MP_OBJ_STOP_ITERATION) {
+    for (;;) {
+        mp_store_obj_result(mp_iternext(iter), &next);
+        if (next == MP_OBJ_STOP_ITERATION) {
+            break;
+        }
         mp_set_lookup(&self->set, next, MP_MAP_LOOKUP_ADD_IF_NOT_FOUND);
     }
 }
diff --git a/py/objsingleton.c b/py/objsingleton.c
index c1f102c94..0b3a983f4 100644
--- a/py/objsingleton.c
+++ b/py/objsingleton.c
@@ -48,7 +48,7 @@ MP_DEFINE_CONST_OBJ_TYPE(
     print, singleton_print
     );
 
-const mp_obj_singleton_t mp_const_ellipsis_obj = {{&mp_type_singleton}, MP_QSTR_Ellipsis};
+const mp_obj_singleton_t MICROPY_OBJ_BASE_ALIGNMENT mp_const_ellipsis_obj = {{&mp_type_singleton}, MP_QSTR_Ellipsis};
 #if MICROPY_PY_BUILTINS_NOTIMPLEMENTED
-const mp_obj_singleton_t mp_const_notimplemented_obj = {{&mp_type_singleton}, MP_QSTR_NotImplemented};
+const mp_obj_singleton_t MICROPY_OBJ_BASE_ALIGNMENT mp_const_notimplemented_obj = {{&mp_type_singleton}, MP_QSTR_NotImplemented};
 #endif
diff --git a/py/objstr.c b/py/objstr.c
index e8b2512a2..7629dac12 100644
--- a/py/objstr.c
+++ b/py/objstr.c
@@ -205,6 +205,22 @@ mp_obj_t mp_obj_str_make_new(const mp_obj_type_t *type, size_t n_args, size_t n_
             mp_print_t print;
             vstr_init_print(&vstr, 16, &print);
             mp_obj_print_helper(&print, args[0], PRINT_STR);
+            #ifdef STRMK_DEBUG
+            {
+                extern void mb_puts(const char *s);
+                extern void mb_puthex32(unsigned long v);
+                union { mp_obj_t o; unsigned char b[4]; } u;
+                mp_obj_t res = mp_obj_new_str_type_from_vstr(type, &vstr);
+                mb_puts("[smk arg=");
+                u.o = args[0];
+                mb_puthex32(u.b[2]); mb_puthex32(u.b[1]); mb_puthex32(u.b[0]);
+                mb_puts(" res=");
+                u.o = res;
+                mb_puthex32(u.b[2]); mb_puthex32(u.b[1]); mb_puthex32(u.b[0]);
+                mb_puts("]\n");
+                return res;
+            }
+            #endif
             return mp_obj_new_str_type_from_vstr(type, &vstr);
         }
 
@@ -2208,7 +2224,7 @@ MP_DEFINE_CONST_OBJ_TYPE(
     );
 
 // The zero-length bytes object, with data that includes a null-terminating byte
-const mp_obj_str_t mp_const_empty_bytes_obj = {{&mp_type_bytes}, 0, 0, (const byte *)""};
+const mp_obj_str_t MICROPY_OBJ_BASE_ALIGNMENT mp_const_empty_bytes_obj = {{&mp_type_bytes}, 0, 0, (const byte *)""};
 
 // Create a str/bytes object using the given data.  New memory is allocated and
 // the data is copied across.  This function should only be used if the type is bytes,
@@ -2459,7 +2475,7 @@ static mp_obj_t bytes_it_iternext(mp_obj_t self_in) {
     }
 }
 
-mp_obj_t mp_obj_new_bytes_iterator(mp_obj_t str, mp_obj_iter_buf_t *iter_buf) {
+static mp_obj_t mp_obj_new_bytes_iterator(mp_obj_t str, mp_obj_iter_buf_t *iter_buf) {
     assert(sizeof(mp_obj_str8_it_t) <= sizeof(mp_obj_iter_buf_t));
     mp_obj_str8_it_t *o = (mp_obj_str8_it_t *)iter_buf;
     o->base.type = &mp_type_polymorph_iter;
diff --git a/py/objstr.h b/py/objstr.h
index 028fc9597..ac5678ee6 100644
--- a/py/objstr.h
+++ b/py/objstr.h
@@ -44,7 +44,7 @@ typedef struct _mp_obj_str_t {
     MP_STATIC_ASSERT(offsetof(mp_obj_str_t, len) == offsetof(mp_obj_array_t, len) \
     && offsetof(mp_obj_str_t, data) == offsetof(mp_obj_array_t, items))
 
-#define MP_DEFINE_STR_OBJ(obj_name, str) mp_obj_str_t obj_name = {{&mp_type_str}, 0, sizeof(str) - 1, (const byte *)str}
+#define MP_DEFINE_STR_OBJ(obj_name, str) mp_obj_str_t MICROPY_OBJ_BASE_ALIGNMENT obj_name = {{&mp_type_str}, 0, sizeof(str) - 1, (const byte *)str}
 
 // use this macro to extract the string hash
 // warning: the hash can be 0, meaning invalid, and must then be explicitly computed from the data
diff --git a/py/objtuple.c b/py/objtuple.c
index c9d8e9b31..5ef555232 100644
--- a/py/objtuple.c
+++ b/py/objtuple.c
@@ -235,7 +235,7 @@ MP_DEFINE_CONST_OBJ_TYPE(
     );
 
 // the zero-length tuple
-const mp_obj_tuple_t mp_const_empty_tuple_obj = {{&mp_type_tuple}, 0};
+const mp_obj_tuple_t MICROPY_OBJ_BASE_ALIGNMENT mp_const_empty_tuple_obj = {{&mp_type_tuple}, 0};
 
 mp_obj_t mp_obj_new_tuple(size_t n, const mp_obj_t *items) {
     if (n == 0) {
diff --git a/py/objtuple.h b/py/objtuple.h
index 892c5771b..20b65a685 100644
--- a/py/objtuple.h
+++ b/py/objtuple.h
@@ -31,16 +31,31 @@
 // type check is done on getiter method to allow tuple, namedtuple, attrtuple
 #define mp_obj_is_tuple_compatible(o) (MP_OBJ_TYPE_GET_SLOT_OR_NULL(mp_obj_get_type(o), iter) == mp_obj_tuple_getiter)
 
+// vbcc strictly enforces C11 6.7.2.1p3: a struct ending in a flexible array
+// member may not be a struct member (mp_obj_namedtuple_t embeds a tuple) and
+// rejects FAM *initializers* (modsys.c's sys.version_info). Calypsi 65816 is
+// worse: it ACCEPTS a FAM initializer and silently emits NOTHING for the
+// array, so every const tuple/attrtuple was 6 bytes of header followed by
+// whatever the linker placed next. A fixed bound makes these ordinary
+// arrays; allocation is offsetof(items)-based so the bound never affects
+// runtime layout, only sizeof of the rare static instances. The bound must
+// cover the largest static initializer (modsys implementation: 3 + fields).
+#if defined(__VBCC__) || defined(__CALYPSI__)
+#define MP_TUPLE_ITEMS_BOUND (4)
+#else
+#define MP_TUPLE_ITEMS_BOUND
+#endif
+
 typedef struct _mp_obj_tuple_t {
     mp_obj_base_t base;
     size_t len;
-    mp_obj_t items[];
+    mp_obj_t items[MP_TUPLE_ITEMS_BOUND];
 } mp_obj_tuple_t;
 
 typedef struct _mp_rom_obj_tuple_t {
     mp_obj_base_t base;
     size_t len;
-    mp_rom_obj_t items[];
+    mp_rom_obj_t items[MP_TUPLE_ITEMS_BOUND];
 } mp_rom_obj_tuple_t;
 
 void mp_obj_tuple_print(const mp_print_t *print, mp_obj_t o_in, mp_print_kind_t kind);
@@ -59,7 +74,7 @@ static inline void mp_obj_tuple_get(mp_obj_t self_in, size_t *len, mp_obj_t **it
 extern const mp_obj_type_t mp_type_attrtuple;
 
 #define MP_DEFINE_ATTRTUPLE(tuple_obj_name, fields, nitems, ...) \
-    const mp_rom_obj_tuple_t tuple_obj_name = { \
+    const mp_rom_obj_tuple_t MICROPY_OBJ_BASE_ALIGNMENT tuple_obj_name = { \
         .base = {.type = &mp_type_attrtuple}, \
         .len = nitems, \
         .items = { __VA_ARGS__, MP_ROM_PTR((void *)fields) } \
diff --git a/py/objtype.c b/py/objtype.c
index 68a36ff98..676bc4b61 100644
--- a/py/objtype.c
+++ b/py/objtype.c
@@ -1043,10 +1043,30 @@ static mp_obj_t type_make_new(const mp_obj_type_t *type_in, size_t n_args, size_
     }
 }
 
+#ifdef TYPECALL_DEBUG
+extern void mb_puts(const char *s);
+extern void mb_puthex32(unsigned long v);
+#endif
 static mp_obj_t type_call(mp_obj_t self_in, size_t n_args, size_t n_kw, const mp_obj_t *args) {
     // instantiate an instance of a class
 
     mp_obj_type_t *self = MP_OBJ_TO_PTR(self_in);
+    #ifdef TYPECALL_DEBUG
+    {
+        union { const void *p; unsigned char b[4]; } u;
+        mb_puts("[tc self=");
+        u.p = self;
+        mb_puthex32(u.b[2]); mb_puthex32(u.b[1]); mb_puthex32(u.b[0]);
+        mb_puts(" idx=");
+        mb_puthex32(self->slot_index_make_new);
+        mb_puts(" fn=");
+        u.p = self->slots[self->slot_index_make_new - 1];
+        mb_puthex32(u.b[2]); mb_puthex32(u.b[1]); mb_puthex32(u.b[0]);
+        mb_puts(" na=");
+        mb_puthex32(n_args);
+        mb_puts("]\n");
+    }
+    #endif
 
     if (!MP_OBJ_TYPE_HAS_SLOT(self, make_new)) {
         #if MICROPY_ERROR_REPORTING <= MICROPY_ERROR_REPORTING_TERSE
@@ -1423,8 +1443,16 @@ MP_DEFINE_CONST_OBJ_TYPE(
     );
 
 void mp_load_super_method(qstr attr, mp_obj_t *dest) {
-    mp_obj_super_t super = {{&mp_type_super}, dest[1], dest[2]};
-    mp_load_method(MP_OBJ_FROM_PTR(&super), attr, dest);
+    // The super object is built on the C stack, so it must satisfy the
+    // object alignment REPR_B requires (bit 0 clear). Calypsi 65816 gives
+    // stack locals no alignment, so align the storage by hand: at an odd
+    // stack address the tagged pointer would read as a small int.
+    byte buf[sizeof(mp_obj_super_t) + 1];
+    mp_obj_super_t *super = (mp_obj_super_t *)(((uintptr_t)&buf[0] & 1) ? &buf[1] : &buf[0]);
+    super->base.type = &mp_type_super;
+    super->type = dest[1];
+    super->obj = dest[2];
+    mp_load_method(MP_OBJ_FROM_PTR(super), attr, dest);
 }
 
 /******************************************************************************/
diff --git a/py/parse.c b/py/parse.c
index 1a50b13b5..df291ee24 100644
--- a/py/parse.c
+++ b/py/parse.c
@@ -215,9 +215,13 @@ typedef struct _rule_stack_t {
 } rule_stack_t;
 
 typedef struct _mp_parse_chunk_t {
-    size_t alloc;
+    // uint32_t rather than size_t: parse-node struct pointers must have their
+    // low 2 bits clear (MP_PARSE_NODE_IS_STRUCT), so data[] must start 4-byte
+    // aligned. On ports with 16-bit size_t and no natural struct padding
+    // (e.g. Calypsi 65816) a size_t here would put data[] at offset 6.
+    uint32_t alloc;
     union {
-        size_t used;
+        uint32_t used;
         struct _mp_parse_chunk_t *next;
     } union_;
     byte data[];
diff --git a/py/parsenum.c b/py/parsenum.c
index 05bcc0f0a..8ec62cd45 100644
--- a/py/parsenum.c
+++ b/py/parsenum.c
@@ -269,7 +269,7 @@ mp_large_float_t mp_decimal_exp(mp_large_float_t num, int dec_exp) {
     //   when the result is expected to be in mp_float_t range (e.g. during format)
     // - we don't need to care about p.exp overflow, as (5.0 ** dec_exp) will anyway
     //   force the final result toward the proper edge if needed (0.0 or inf)
-    res.p.exp += dec_exp;
+    MP_FLOAT_UNION_ADD_EXP(res, dec_exp);
     // Use positive exponents when they are more precise then negative
     if (dec_exp < 0 && dec_exp >= -MAX_EXACT_POWER_OF_5) {
         res.f /= MICROPY_FLOAT_C_FUN(pow)(5, -dec_exp);
diff --git a/py/qstr.c b/py/qstr.c
index 7902646a9..e9d395715 100644
--- a/py/qstr.c
+++ b/py/qstr.c
@@ -103,6 +103,16 @@ const qstr_len_t mp_qstr_const_lengths_static[] = {
     #endif
 };
 
+static const char *const mp_qstr_const_qstrs_static[] = {
+    #ifndef NO_QSTR
+#define QDEF0(id, hash, len, str) str,
+#define QDEF1(id, hash, len, str)
+    #include "genhdr/qstrdefs.generated.h"
+#undef QDEF0
+#undef QDEF1
+    #endif
+};
+
 const qstr_pool_t mp_qstr_const_pool_static = {
     NULL,               // no previous pool
     0,                  // no previous pool
@@ -113,15 +123,7 @@ const qstr_pool_t mp_qstr_const_pool_static = {
     (qstr_hash_t *)mp_qstr_const_hashes_static,
     #endif
     (qstr_len_t *)mp_qstr_const_lengths_static,
-    {
-        #ifndef NO_QSTR
-#define QDEF0(id, hash, len, str) str,
-#define QDEF1(id, hash, len, str)
-        #include "genhdr/qstrdefs.generated.h"
-#undef QDEF0
-#undef QDEF1
-        #endif
-    },
+    mp_qstr_const_qstrs_static,
 };
 
 // The next pool is the remainder of the qstrs defined in the firmware. This
@@ -148,6 +150,16 @@ const qstr_len_t mp_qstr_const_lengths[] = {
     #endif
 };
 
+static const char *const mp_qstr_const_qstrs[] = {
+    #ifndef NO_QSTR
+#define QDEF0(id, hash, len, str)
+#define QDEF1(id, hash, len, str) str,
+    #include "genhdr/qstrdefs.generated.h"
+#undef QDEF0
+#undef QDEF1
+    #endif
+};
+
 const qstr_pool_t mp_qstr_const_pool = {
     &mp_qstr_const_pool_static,
     MP_QSTRnumber_of_static,
@@ -158,15 +170,7 @@ const qstr_pool_t mp_qstr_const_pool = {
     (qstr_hash_t *)mp_qstr_const_hashes,
     #endif
     (qstr_len_t *)mp_qstr_const_lengths,
-    {
-        #ifndef NO_QSTR
-#define QDEF0(id, hash, len, str)
-#define QDEF1(id, hash, len, str) str,
-        #include "genhdr/qstrdefs.generated.h"
-#undef QDEF0
-#undef QDEF1
-        #endif
-    },
+    mp_qstr_const_qstrs,
 };
 
 // If frozen code is enabled, then there is an additional, sorted, ROM pool
@@ -232,6 +236,7 @@ static qstr qstr_add(mp_uint_t len, const char *q_ptr) {
             QSTR_EXIT();
             m_malloc_fail(new_alloc);
         }
+        pool->qstrs = (const char *const *)(pool + 1);
         #if MICROPY_QSTR_BYTES_IN_HASH
         pool->hashes = (qstr_hash_t *)(pool->qstrs + new_alloc);
         pool->lengths = (qstr_len_t *)(pool->hashes + new_alloc);
@@ -252,7 +257,7 @@ static qstr qstr_add(mp_uint_t len, const char *q_ptr) {
     MP_STATE_VM(last_pool)->hashes[at] = hash;
     #endif
     MP_STATE_VM(last_pool)->lengths[at] = len;
-    MP_STATE_VM(last_pool)->qstrs[at] = q_ptr;
+    ((const char **)MP_STATE_VM(last_pool)->qstrs)[at] = q_ptr;
     MP_STATE_VM(last_pool)->len++;
 
     // return id for the newly-added qstr
diff --git a/py/qstr.h b/py/qstr.h
index 0cde6062e..150080554 100644
--- a/py/qstr.h
+++ b/py/qstr.h
@@ -88,7 +88,10 @@ typedef struct _qstr_pool_t {
     qstr_hash_t *hashes;
     #endif
     qstr_len_t *lengths;
-    const char *qstrs[];
+    // A pointer rather than a flexible array member: Calypsi 5.17 silently
+    // drops FAM initializer data (see ports/snes DECISIONS.md). Const pools
+    // point at named arrays; runtime pools point just past the struct.
+    const char *const *qstrs;
 } qstr_pool_t;
 
 #define QSTR_TOTAL() (MP_STATE_VM(last_pool)->total_prev_len + MP_STATE_VM(last_pool)->len)
diff --git a/py/runtime.c b/py/runtime.c
index 618e9b5ae..71b14e26c 100644
--- a/py/runtime.c
+++ b/py/runtime.c
@@ -59,7 +59,7 @@
 #define DEBUG_OP_printf(...) (void)0
 #endif
 
-const mp_obj_module_t mp_module___main__ = {
+const mp_obj_module_t MICROPY_OBJ_BASE_ALIGNMENT mp_module___main__ = {
     .base = { &mp_type_module },
     .globals = (mp_obj_dict_t *)&MP_STATE_VM(dict_main),
 };
@@ -1297,6 +1297,14 @@ void mp_store_attr(mp_obj_t base, qstr attr, mp_obj_t value) {
 
 mp_obj_t mp_getiter(mp_obj_t o_in, mp_obj_iter_buf_t *iter_buf) {
     assert(o_in);
+    // The iter_buf is usually a C stack local, and it becomes an mp_obj_t:
+    // it must satisfy the tagged-pointer alignment (REPR_B: bit 0 clear).
+    // Compilers that don't align stack locals (Calypsi 65816) can pass an
+    // odd buffer, which would be misread as a small int; fall back to heap
+    // allocation in that case. Dead code on aligned platforms.
+    if (((uintptr_t)iter_buf & 1) != 0) {
+        iter_buf = NULL;
+    }
     const mp_obj_type_t *type = mp_obj_get_type(o_in);
 
     // Most types that use iternext just use the identity getiter. We handle this case explicitly
diff --git a/py/runtime_utils.c b/py/runtime_utils.c
index 3b0ae279a..96786c970 100644
--- a/py/runtime_utils.c
+++ b/py/runtime_utils.c
@@ -52,6 +52,9 @@ mp_obj_t mp_call_function_2_protected(mp_obj_t fun, mp_obj_t arg1, mp_obj_t arg2
 }
 
 #if !MICROPY_USE_GCC_MUL_OVERFLOW_INTRINSIC
+#if !defined(__VBCC__)
+// vbcc 65816 has no 64-bit runtime; this long-long variant is unused with
+// LONGINT_IMPL NONE. mp_mul_mp_int_t_overflow below is 32-bit and is kept.
 bool mp_mul_ll_overflow(long long int x, long long int y, long long int *res) {
     bool overflow;
 
@@ -76,6 +79,7 @@ bool mp_mul_ll_overflow(long long int x, long long int y, long long int *res) {
 
     return overflow;
 }
+#endif // !__VBCC__
 
 bool mp_mul_mp_int_t_overflow(mp_int_t x, mp_int_t y, mp_int_t *res) {
     // Check for multiply overflow; see CERT INT32-C
diff --git a/tools/mpy-tool.py b/tools/mpy-tool.py
index 67e2cbf15..d57fd5afa 100755
--- a/tools/mpy-tool.py
+++ b/tools/mpy-tool.py
@@ -745,7 +745,7 @@ class CompiledModule:
             else:
                 obj_type = "mp_type_bytes"
             print(
-                'static const mp_obj_str_t %s = {{&%s}, %u, %u, (const byte*)"%s"};'
+                'static const mp_obj_str_t MICROPY_OBJ_BASE_ALIGNMENT %s = {{&%s}, %u, %u, (const byte*)"%s"};'
                 % (
                     obj_name,
                     obj_type,
@@ -764,7 +764,7 @@ class CompiledModule:
             elif config.MICROPY_LONGINT_IMPL == config.MICROPY_LONGINT_IMPL_NONE:
                 raise FreezeError(self, "target does not support long int")
             elif config.MICROPY_LONGINT_IMPL == config.MICROPY_LONGINT_IMPL_LONGLONG:
-                print("static const mp_obj_int_t %s = {{&mp_type_int}, %d};" % (obj_name, obj))
+                print("static const mp_obj_int_t MICROPY_OBJ_BASE_ALIGNMENT %s = {{&mp_type_int}, %d};" % (obj_name, obj))
                 return "MP_ROM_PTR(&%s)" % obj_name
             elif config.MICROPY_LONGINT_IMPL == config.MICROPY_LONGINT_IMPL_MPZ:
                 neg = 0
@@ -780,7 +780,7 @@ class CompiledModule:
                 ndigs = len(digs)
                 digs = ",".join(("%#x" % d) for d in digs)
                 print(
-                    "static const mp_obj_int_t %s = {{&mp_type_int}, "
+                    "static const mp_obj_int_t MICROPY_OBJ_BASE_ALIGNMENT %s = {{&mp_type_int}, "
                     "{.neg=%u, .fixed_dig=1, .alloc=%u, .len=%u, .dig=(uint%u_t*)(const uint%u_t[]){%s}}};"
                     % (obj_name, neg, ndigs, ndigs, bits_per_dig, bits_per_dig, digs)
                 )
@@ -793,7 +793,7 @@ class CompiledModule:
                 "#if MICROPY_OBJ_REPR == MICROPY_OBJ_REPR_A || MICROPY_OBJ_REPR == MICROPY_OBJ_REPR_B"
             )
             print(
-                "static const mp_obj_float_t %s = {{&mp_type_float}, (mp_float_t)%.16g};"
+                "static const mp_obj_float_t MICROPY_OBJ_BASE_ALIGNMENT %s = {{&mp_type_float}, (mp_float_t)%.16g};"
                 % (obj_name, obj)
             )
             print("#define %s MP_ROM_PTR(&%s)" % (macro_name, obj_name))
@@ -810,7 +810,7 @@ class CompiledModule:
             return macro_name
         elif isinstance(obj, complex):
             print(
-                "static const mp_obj_complex_t %s = {{&mp_type_complex}, (mp_float_t)%.16g, (mp_float_t)%.16g};"
+                "static const mp_obj_complex_t MICROPY_OBJ_BASE_ALIGNMENT %s = {{&mp_type_complex}, (mp_float_t)%.16g, (mp_float_t)%.16g};"
                 % (obj_name, obj.real, obj.imag)
             )
             return "MP_ROM_PTR(&%s)" % obj_name
@@ -823,7 +823,7 @@ class CompiledModule:
                     sub_obj_name = "%s_%u" % (obj_name, i)
                     obj_refs.append(self.freeze_constant_obj(sub_obj_name, sub_obj))
                 print(
-                    "static const mp_rom_obj_tuple_t %s = {{&mp_type_tuple}, %d, {"
+                    "static const mp_rom_obj_tuple_t MICROPY_OBJ_BASE_ALIGNMENT %s = {{&mp_type_tuple}, %d, {"
                     % (obj_name, len(obj))
                 )
                 for ref in obj_refs:
@@ -1676,6 +1676,11 @@ def freeze_mpy(firmware_qstr_idents, compiled_modules):
         qstr_content += len(qbytes) + 1  # include NUL
     print("};")
     print()
+    print("static const char *const mp_qstr_frozen_const_qstrs[] = {")
+    for _, _, qstr, qbytes in new:
+        print('    "%s",' % qstrutil.escape_bytes(qstr, qbytes))
+    print("};")
+    print()
     print("extern const qstr_pool_t mp_qstr_const_pool;")
     print("const qstr_pool_t mp_qstr_frozen_const_pool = {")
     print("    &mp_qstr_const_pool, // previous pool")
@@ -1686,10 +1691,7 @@ def freeze_mpy(firmware_qstr_idents, compiled_modules):
     if config.MICROPY_QSTR_BYTES_IN_HASH:
         print("    (qstr_hash_t *)mp_qstr_frozen_const_hashes,")
     print("    (qstr_len_t *)mp_qstr_frozen_const_lengths,")
-    print("    {")
-    for _, _, qstr, qbytes in new:
-        print('        "%s",' % qstrutil.escape_bytes(qstr, qbytes))
-    print("    },")
+    print("    mp_qstr_frozen_const_qstrs,")
     print("};")
 
     # Freeze all modules.
